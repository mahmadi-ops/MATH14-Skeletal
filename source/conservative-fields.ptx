<?xml version="1.0" encoding="utf-8"?>
<!-- MATH 14 Skeletal Notes                                                -->
<!-- Section: Path Independence, Conservative Fields, and Potential        -->
<!-- Functions                                                             -->
<!--                                                                       -->
<!-- Skeletal (fill-in) version of the section of the same name in the     -->
<!-- completed MATH 14 notes.  The definitions, the three theorem          -->
<!-- statements, and the equivalence square are kept; the three proofs,    -->
<!-- the gradient of the paraboloid, the four worked examples, and the     -->
<!-- three Concept Check solutions are replaced by blank workspace boxes,  -->
<!-- each preceded by a one-line instruction.  File name and xml:ids are   -->
<!-- unchanged.                                                            -->
<!--                                                                       -->
<!-- Figures: every diagram that illustrates a definition or a theorem     -->
<!-- (the gradient field, simply connected regions, the region D with its  -->
<!-- curve, the three paths, the split loop, the equivalence square), the  -->
<!-- two-paths picture of Example I, the video, and the three GeoGebra     -->
<!-- applets are kept.  The three-step cascade that displayed the          -->
<!-- potential function of Example III is dropped, since it was the        -->
<!-- answer.  Captions that stated a numerical answer have been trimmed.   -->
<section xml:id="section-conservative-fields" xmlns:xi="http://www.w3.org/2001/XInclude">
<title>Path Independence, Conservative Fields, and Potential Functions</title>

<p>
Before we begin discussing the actual topic of today's lecture, we will
need to study a particular type of a vector field known as a
<em>gradient field</em>.
</p>

<!-- ================================================================ -->
<subsection xml:id="sec-gradient-fields">
<title>A Brief Reminder (Gradient Fields)</title>

<definition xml:id="def-gradient-field">
<title>Gradient Field</title>
<statement>
<p>
The <term>gradient field</term> of a differentiable function
<m>f(x,y,z)</m> is the field of gradient vectors
</p>

<men xml:id="eq-gradient-field">
\nabla f = \frac{\partial f}{\partial x}\mathbf i
+ \frac{\partial f}{\partial y}\mathbf j
+ \frac{\partial f}{\partial z}\mathbf k.
</men>
</statement>
</definition>

<p>
At each point <m>(x,y,z)</m>, the gradient field gives a vector pointing
in the direction of greatest increase of <m>f</m>. Let us emphasize that
even though the function <m>f</m> is a scalar-valued function, its
gradient is a vector-valued one by definition. As an example, see
<xref ref="fig-gradient-field-tikz"/>.
</p>

<figure xml:id="fig-gradient-field-tikz">
<caption>
The surface <m>z = f(x,y) = x^2 + y^2</m> together with its gradient
field <m>\nabla f = 2x\,\mathbf i + 2y\,\mathbf j</m>, drawn in the
<m>xy</m>-plane. The gradient vectors point radially away from the
origin, the direction of steepest ascent, and grow longer as we move
away from the origin.
</caption>
<image width="75%">
<shortdescription>
A paraboloid above the plane, with gradient arrows below it pointing radially outward.
</shortdescription>
<description>
An upward-opening paraboloid, the graph of z equals x squared plus y
squared, drawn as a translucent blue surface. In the x y plane below
it, blue arrows of the gradient field point radially outward from the
origin, growing longer at points farther from the origin.
</description>
<latex-image>
<![CDATA[\begin{tikzpicture}[line join=round, line cap=round]
  % Okabe-Ito colorblind-safe palette
  \definecolor{oiblue}{RGB}{0,114,178}
  \definecolor{oivermillion}{RGB}{213,94,0}
  \definecolor{oigreen}{RGB}{0,158,115}
  \definecolor{oiyellow}{RGB}{240,228,66}
  \definecolor{oipurple}{RGB}{204,121,167}
  \begin{axis}[
      view={115}{22},
      width=13cm,
      axis lines=center,
      axis line style={-latex, black!75, line width=0.7pt},
      xlabel={$x$}, ylabel={$y$}, zlabel={$z$},
      label style={font=\large},
      ticks=none,
      xmin=-3.4, xmax=3.4, ymin=-3.4, ymax=3.4, zmin=0, zmax=5.4,
      clip=false,
    ]
    % ---- gradient field 2x i + 2y j in the xy-plane ----
    % arrows at radii .6, 1.2, 1.8, 2.4; length scaled by 0.22*r
    \foreach \ang in {0,30,...,330}{
      \foreach \rr in {0.6,1.2,1.8,2.4}{
        \edef\tmp{\noexpand\draw[-{Latex[length=1.6mm]}, oiblue,
            line width=0.9pt]
          (axis cs:{\rr*cos(\ang)},{\rr*sin(\ang)},0) --
          (axis cs:{(\rr+0.44*\rr)*cos(\ang)},{(\rr+0.44*\rr)*sin(\ang)},0);}
        \tmp}}
    % ---- surface: paraboloid z = (x^2 + y^2)/2 (scaled for display) ----
    \addplot3[surf, shader=interp,
              point meta={z},
              colormap={softz}{color=(oiblue!30!white) color=(oiblue!80!white)},
              opacity=0.75, fill opacity=0.75,
              domain=0:360, samples=61,
              y domain=0:2.1, samples y=25,
              z buffer=sort, forget plot]
      ({y*cos(x)}, {y*sin(x)}, {y^2});
    % level curves (circles) for shape cues
    \foreach \rr in {0.7,1.2,1.7,2.1}{
      \edef\tmp{\noexpand\addplot3[black!70, line width=1.1pt, domain=0:360,
          samples=97, samples y=0, forget plot]
        ({\rr*cos(x)}, {\rr*sin(x)}, {\rr*\rr});}\tmp}
    % labels (placed relative to the picture, away from the axes)
    \node[font=\large, anchor=north west] at (axis description cs:0.0,0.97)
      {$z = f(x,y) = x^2 + y^2$};
    \node[oiblue, font=\small, anchor=south east]
      at (axis description cs:1.0,0.04)
      {$\nabla f = 2x\,\mathbf i + 2y\,\mathbf j$};
  \end{axis}
\end{tikzpicture}]]>
</latex-image>
</image>
</figure>

<p>
In <xref ref="fig-gradient-field-tikz"/>, you see the scalar-valued
function <m>z = f(x,y) = x^2 + y^2</m>, which is a function on
<m>\R^2</m>. Compute its gradient according to
<xref ref="eq-gradient-field"/>, and compare with the arrows in the
figure.
</p>

<sidebyside width="97%">
<image>
<latex-image>
\begin{tikzpicture}
\draw[rounded corners=8pt, draw=gray!55, thick] (0,0) rectangle (15,3.5);
\node[anchor=north west, font=\small\itshape] at (0.35,3.2) {$\nabla f =$};
\end{tikzpicture}
</latex-image>
<shortdescription>Blank box with one row, for the gradient of x squared plus y squared.</shortdescription>
</image>
</sidebyside>

<p>
The gradient field is visualized in the <m>xy</m>-plane and the value of
the function <m>f(x,y) = x^2 + y^2</m> at each point <m>(x,y)</m> is
plotted on the <m>z</m>-axis. Intuitively, the gradient of a function
<m>f(x,y)</m> at a point <m>(x_0,y_0)</m> is a vector pointing in the
direction of the steepest slope.
</p>

</subsection>

<!-- ================================================================ -->
<subsection xml:id="sec-conservative-definitions">
<title>Path Independence and Some Basic Definitions</title>

<p>
Previously, in <xref ref="ex-work-path-dependence"/>, we saw that the
line integral of a vector field depends on the path in general. Then,
the natural question to ask is:
</p>

<blockquote>
<p>
<em>What are the conditions under which the line integral of a vector
field <m>\mathbf F</m> from point <m>A</m> to point <m>B</m> is
independent of the curve <m>C</m> taken to go from <m>A</m> to
<m>B</m>?</em>
</p>
</blockquote>

<p>
Answering this question requires a few new ideas, so we introduce them
first.
</p>

<remark xml:id="remark-smooth-curve">
<title>Reminder</title>
<p>
A <term>smooth curve</term> has no sharp corners or cusps; when the
tangent vector turns, it does so continuously.
</p>
</remark>

<definition xml:id="def-conservative-concepts">
<title>Open, Connected, and Simply Connected Regions; Conservative Fields</title>
<statement>
<p>
<ul>
<li>
<p>
A region <m>D</m> is <term>open</term> if for every point <m>P</m> in
<m>D</m> there is a disk with center <m>P</m> that lies entirely in
<m>D</m>, which means that <m>D</m> doesn't contain any of its boundary
points.
</p>
</li>
<li>
<p>
Consider an open region <m>D</m> in space and a vector field
<m>\mathbf F</m>. If the result of the line integral
<m>\int_C \mathbf F\cdot d\mathbf r</m> from <m>A</m> to <m>B</m> is the
same for all possible paths inside <m>D</m>, then the line integral
<m>\int_C \mathbf F\cdot d\mathbf r</m> is said to be
<term>path-independent</term> inside <m>D</m> and the field
<m>\mathbf F</m> is said to be <term>conservative</term> on <m>D</m>.
</p>
</li>
<li>
<p>
If a vector field <m>\mathbf F</m> can be written as
<m>\mathbf F = \nabla f</m>, where <m>f</m> is a scalar-valued function,
then <m>f</m> is a <term>potential function</term>.
</p>
</li>
<li>
<p>
<term>Piecewise smooth</term> curves are curves that are made up of
finitely many smooth pieces.
</p>
</li>
<li>
<p>
An open region <m>D</m> is said to be <term>connected</term> if any two
points in <m>D</m> can be joined by a smooth curve that lies in the
region.
</p>
</li>
<li>
<p>
<m>D</m> is <term>simply connected</term>, which means that every loop
in <m>D</m> can be contracted to a point in <m>D</m> without ever
leaving <m>D</m>. (See <xref ref="fig-simply-connected-prefigure"/>.)
</p>
</li>
</ul>
</p>
</statement>
</definition>

<figure xml:id="fig-simply-connected-prefigure">
<caption>
On the left, a simply connected region: every loop can be contracted to
a point without leaving the region. On the right, a region that is not
simply connected: the loop <m>C_1</m> encircles a hole and cannot be
contracted to a point while staying inside the region.
</caption>
<sidebyside widths="48% 48%">
<image>
<shortdescription>
A blob-shaped region with no holes.
</shortdescription>
<prefigure xmlns="https://prefigure.org" label="prefigure-simply-connected">
<diagram dimensions="(300,260)" margins="10">
<definition>blob(t) = ((1.5 + 0.55*sin(2*t) + 0.3*cos(3*t))*cos(t) + 0.2,
(1.2 + 0.45*cos(2*t) + 0.25*sin(3*t))*sin(t) + 0.4)</definition>
<coordinates bbox="(-2.9,-2.2,2.9,2.8)">
<axes at="sc-axes" xlabel="x" ylabel="y" decorations="no" arrows="1"/>
<parametric-curve at="sc-region" function="blob" domain="(0,2*pi)"
    closed="yes" fill="#f4e3c1" stroke="#56B4E9" thickness="2"/>
<label at="sc-label" anchor="(1.5,-1.75)" alignment="s">
Simply connected
</label>
</coordinates>
<annotations>
<annotation ref="figure" text="A blob shaped region in the x y plane with no holes, filled in tan with a light blue boundary. The region is labeled simply connected.">
  <annotation ref="sc-region" text="A blob shaped region with no holes. Any loop drawn inside this region can be contracted to a point without leaving the region."/>
</annotation>
</annotations>
</diagram>
</prefigure>
</image>
<image>
<shortdescription>
A blob-shaped region with two holes and a loop around one hole.
</shortdescription>
<prefigure xmlns="https://prefigure.org" label="prefigure-not-simply-connected">
<diagram dimensions="(300,260)" margins="10">
<definition>blob(t) = ((1.9 + 0.4*sin(2*t))*cos(t),
(1.25 + 0.3*cos(3*t))*sin(t) + 0.3)</definition>
<coordinates bbox="(-2.9,-2.2,2.9,2.8)">
<axes at="nsc-axes" xlabel="x" ylabel="y" decorations="no" arrows="1"/>
<parametric-curve at="nsc-region" function="blob" domain="(0,2*pi)"
    closed="yes" fill="#f4e3c1" stroke="#56B4E9" thickness="2"/>
<circle at="nsc-hole1" center="(-0.7,0.4)" radius="0.32" fill="white"
    stroke="#56B4E9" thickness="2"/>
<circle at="nsc-hole2" center="(1.1,0.5)" radius="0.28" fill="white"
    stroke="#56B4E9" thickness="2"/>
<circle at="nsc-loop" center="(-0.7,0.4)" radius="0.62" fill="none"
    stroke="#D55E00" thickness="2"/>
<vector at="nsc-loop-arrow" v="(-0.28,0)" tail="(-0.56,1.02)"
    stroke="#D55E00" thickness="2"/>
<label at="nsc-loop-label" anchor="(-0.7,-0.45)" alignment="s"
    color="#D55E00"><m>C_1</m></label>
<label at="nsc-label" anchor="(1.56,-1.7)" alignment="s">
Not simply connected
</label>
</coordinates>
<annotations>
<annotation ref="figure" text="A blob shaped region in the x y plane containing two white holes. A vermillion loop labeled C 1 encircles the left hole, so it cannot be contracted to a point without leaving the region.">
  <annotation ref="nsc-region" text="A blob shaped region filled in tan with a light blue boundary."/>
  <annotation ref="nsc-hole1" text="A hole in the region: a white disk that does not belong to the region."/>
  <annotation ref="nsc-hole2" text="A second hole in the region."/>
  <annotation ref="nsc-loop" text="A vermillion loop labeled C 1 that encircles the left hole. Contracting this loop to a point would force it to cross the hole and leave the region, so the region is not simply connected."/>
</annotation>
</annotations>
</diagram>
</prefigure>
</image>
</sidebyside>
</figure>

</subsection>

<!-- ================================================================ -->
<subsection xml:id="sec-ftli">
<title>Line Integrals in Conservative Fields</title>

<p>
Next, we will study the line integrals of conservative fields, in the
sense of <xref ref="def-conservative-concepts"/>. As you
will recognize, if you think of the gradient of a vector field as its
derivative, then the fundamental theorem of line integrals makes a
similar statement for vector-valued functions as the one made for
definite integrals of scalar-valued functions, i.e., the one that you
know as FTC II.<fn>The Fundamental Theorem of Calculus, Part II: if
<m>f</m> is continuous on <m>[a,b]</m> and <m>F</m> is any antiderivative
of <m>f</m> on that interval, then
<m>\int_a^b f(x)\,dx = F(b) - F(a)</m>. It evaluates a definite integral
from the values of an antiderivative at the two endpoints, and
<xref ref="thm-ftli"/> does the same for a line integral, using the
values of a potential function at the two endpoints of the
curve.</fn>
</p>

<theorem xml:id="thm-ftli">
<title>Fundamental Theorem of Line Integrals</title>
<statement>
<p>
Let <m>C</m> be a smooth curve joining the point <m>A</m> to the point
<m>B</m> in the plane or in space and parametrized by
<m>\mathbf r(t)</m>. Let <m>f</m> be a differentiable function with a
continuous gradient vector <m>\mathbf F = \nabla f</m> on a domain
<m>D</m> containing <m>C</m>. Then
</p>

<men xml:id="eq-ftli">
\int_C \mathbf F\cdot d\mathbf r = f(B) - f(A).
</men>
</statement>
<proof>
<p>
(Parametrize <m>C</m> by
<m>\mathbf r(t) = x(t)\mathbf i + y(t)\mathbf j + z(t)\mathbf k</m>,
<m>a \le t \le b</m>, and write the line integral in the parametrized
form <xref ref="eq-work-parametrized"/> with
<m>\mathbf F = \nabla f</m>. Expand the dot product of the gradient
with <m>d\mathbf r/dt</m>; the chain rule turns the integrand into a
single derivative with respect to <m>t</m>, and the Fundamental Theorem
of Calculus finishes the job.)
</p>

<sidebyside width="97%">
<image>
<latex-image>
\begin{tikzpicture}
\draw[rounded corners=8pt, draw=gray!55, thick] (0,0) rectangle (15,12);
\draw[gray!35] (0,8.5) -- (15,8.5);
\draw[gray!35] (0,5) -- (15,5);
\node[anchor=north west, font=\small\itshape] at (0.35,11.7) {$\displaystyle\int_C \mathbf F\cdot d\mathbf r = \int_a^b \nabla f \cdot \frac{d\mathbf r}{dt}\,dt =$};
\node[anchor=north west, font=\small\itshape] at (0.35,8.2) {the integrand is, by the chain rule,};
\node[anchor=north west, font=\small\itshape] at (0.35,4.7) {so the integral equals};
\end{tikzpicture}
</latex-image>
<shortdescription>Blank box in three rows, for expanding the dot product of the gradient with the velocity, for recognizing the integrand as a derivative with respect to t by the chain rule, and for evaluating the integral.</shortdescription>
</image>
</sidebyside>
</proof>
</theorem>

<p>
In <xref ref="fig-ftli-region-tikz"/>, the region <m>D</m> is drawn as a
translucent solid and the curve <m>C</m> is in red. According
to the fundamental theorem, the line integral of a conservative vector
field <m>\mathbf F</m> can be easily calculated by calculating the
values of its corresponding potential function <m>f</m>
(<xref ref="def-conservative-concepts"/>) at the beginning
(point <m>A</m>) and end (point <m>B</m>) of the curve <m>C</m>.
</p>

<figure xml:id="fig-ftli-region-tikz">
<caption>
A smooth curve <m>C</m> joining <m>A(x_1,y_1,z_1)</m> to
<m>B(x_2,y_2,z_2)</m> inside an open region <m>D</m> in space, drawn as
a translucent solid containing <m>C</m>. For a conservative field
<m>\mathbf F = \nabla f</m>, the line integral along <m>C</m> equals
<m>f(B) - f(A)</m>, no matter how <m>C</m> winds from <m>A</m> to
<m>B</m>.
</caption>
<image width="70%">
<shortdescription>
A curve winding from point A to point B inside a dotted region in space.
</shortdescription>
<description>
Three coordinate axes in space with a translucent blue solid marking an
open region D. Inside the solid, a red curve C winds from the point A
with coordinates x 1 comma y 1 comma z 1 to the point B with
coordinates x 2 comma y 2 comma z 2, with an arrowhead at the middle of
the curve showing the direction of travel from A to B.
</description>
<latex-image>
<![CDATA[\begin{tikzpicture}[line join=round, line cap=round]
  % Okabe-Ito colorblind-safe palette
  \definecolor{oiblue}{RGB}{0,114,178}
  \definecolor{oivermillion}{RGB}{213,94,0}
  \definecolor{oigreen}{RGB}{0,158,115}
  \definecolor{oipurple}{RGB}{204,121,167}
  \begin{axis}[
      view={115}{20},
      width=12cm,
      axis lines=center,
      axis line style={-latex, black!75, line width=0.7pt},
      xlabel={$x$}, ylabel={$y$}, zlabel={$z$},
      label style={font=\large},
      ticks=none,
      xmin=-0.4, xmax=2.7, ymin=-0.6, ymax=3.3, zmin=-0.2, zmax=2.7,
      clip=false,
    ]
    % ---- the region D: a translucent solid (ellipsoid) containing C ----
    \addplot3[surf, shader=interp, draw=none,
        colormap={dblue}{color=(oiblue!12!white) color=(oiblue!55!white)},
        opacity=0.30, fill opacity=0.30,
        domain=0:360, samples=61,
        y domain=-90:90, samples y=31,
        z buffer=sort, forget plot]
      ({1.25 + 1.2*cos(y)*cos(x)},
       {1.35 + 1.75*cos(y)*sin(x)},
       {1.15 + 1.1*sin(y)});
    % parallels and meridians, so D reads as a solid rather than a flat disc
    \foreach \vv in {-55,-25,10,45}{
      \edef\tmp{\noexpand\addplot3[oiblue!55, line width=0.5pt, domain=0:360,
          samples=97, samples y=0, forget plot]
        ({1.25 + 1.2*cos(\vv)*cos(x)}, {1.35 + 1.75*cos(\vv)*sin(x)},
         {1.15 + 1.1*sin(\vv)});}\tmp}
    \foreach \uu in {0,30,...,330}{
      \edef\tmp{\noexpand\addplot3[oiblue!40, line width=0.4pt, domain=-90:90,
          samples=49, samples y=0, forget plot]
        ({1.25 + 1.2*cos(x)*cos(\uu)}, {1.35 + 1.75*cos(x)*sin(\uu)},
         {1.15 + 1.1*sin(x)});}\tmp}
    \node[anchor=south, font=\large] at (axis cs:1.25,1.35,2.32) {$D$};
    % ---- the curve C from A to B (a gentle S-curve, in red/vermillion) ----
    \addplot3[oivermillion, line width=1.7pt,
        domain=0:1, samples=81, samples y=0, forget plot]
      ({0.9 + 0.5*x + 0.45*sin(360*x)},
       {0.15 + 2.45*x},
       {0.6 + 1.1*x});
    % direction arrowhead placed near the middle of C
    \addplot3[oivermillion, line width=1.7pt, -{Latex[length=2.6mm]},
        domain=0.40:0.50, samples=21, samples y=0, forget plot]
      ({0.9 + 0.5*x + 0.45*sin(360*x)},
       {0.15 + 2.45*x},
       {0.6 + 1.1*x});
    \node[oivermillion, anchor=south east, font=\large]
      at (axis cs:1.05,1.5,1.45) {$C$};
    % ---- endpoints ----
    \addplot3[only marks, mark=*, mark size=1.8pt, color=oiblue,
        forget plot] coordinates {(0.9,0.15,0.6) (1.4,2.6,1.7)};
    \node[anchor=south, xshift=-8pt, font=\small] at (axis cs:0.9,0.15,0.68)
      {$A(x_1,y_1,z_1)$};
    \node[anchor=south west, font=\small] at (axis cs:1.42,2.62,1.78)
      {$B(x_2,y_2,z_2)$};
  \end{axis}
\end{tikzpicture}]]>
</latex-image>
</image>
</figure>

<p>
The central idea of the theorem<mdash/>that only the endpoints
matter<mdash/>is animated in <xref ref="fig-ftli-video"/>.
</p>

<figure xml:id="fig-ftli-video">
<caption>
Animation of the fundamental theorem of line integrals for the field
<m>\mathbf F = \nabla f</m> with
<m>f(x,y,z) = 1/\sqrt{x^2+y^2+z^2}</m>. Two different curves join
<m>(1,0,0)</m> to <m>(1,0,2\pi)</m>: the helix <m>C_1</m> and the
straight segment <m>C_2</m>. The accumulated value of each line
integral is tracked as the paths are traced, and both integrals arrive
at the same value <m>f(B) - f(A)</m>.
</caption>
<video preview="play-button.png" xml:id="video-ftli-path-independence"
       source="videos/ftli_path_independence_1080p.mp4"
       width="70%" aspect="16:9"/>
</figure>

<p>
In the next example, we will see how we can use
<xref ref="thm-ftli"/>.
</p>

<example xml:id="example-ftli-helix">
<title>Example I</title>
<statement>
<p>
Consider the scalar-valued function
<m>f(x,y,z) = \dfrac{1}{\sqrt{x^2 + y^2 + z^2}}</m>.
</p>

<p>
<ol marker="A.">
<li>
<p>
Calculate the gradient vector field
<m>\mathbf F(x,y,z) = \nabla f</m>.
</p>
</li>
<li>
<p>
Evaluate the line integral of the vector field
<m>\mathbf F(x,y,z) = \nabla f</m> over the curve <m>C_1</m>
parametrized as
<m>\mathbf r(t) = \cos(t)\mathbf i + \sin(t)\mathbf j + t\mathbf k</m>
from <m>(1,0,0)</m> to <m>(1,0,2\pi)</m>.
</p>
</li>
<li>
<p>
What is the result of <m>\int_{C_2} \mathbf F\cdot d\mathbf r</m> from
<m>(1,0,0)</m> to <m>(1,0,2\pi)</m> where <m>C_2</m> is the line
connecting the two points?
</p>
</li>
</ol>
</p>
<p>
<em>Solution.</em>
(In <alert>A</alert>, write <m>f = \left(x^2+y^2+z^2\right)^{-1/2}</m>
and take the three partial derivatives; you should recognize the field.
In <alert>B</alert>, one way would be to parametrize, exactly as in
<xref ref="ex-helix-gravity"/>; instead use <xref ref="thm-ftli"/>,
which needs only the values of <m>f</m> at the two endpoints. In
<alert>C</alert>, say what <xref ref="thm-ftli"/> tells you about
<m>C_2</m> without computing anything, and why. The two curves are shown
in <xref ref="fig-helix-segment-tikz"/>.)
</p>

<sidebyside width="97%">
<image>
<latex-image>
\begin{tikzpicture}
\draw[rounded corners=8pt, draw=gray!55, thick] (0,0) rectangle (15,14);
\draw[gray!35] (0,8.5) -- (15,8.5);
\draw[gray!35] (0,4) -- (15,4);
\node[anchor=north west, font=\small\itshape] at (0.35,13.7) {A. $\mathbf F = \nabla f =$};
\node[anchor=north west, font=\small\itshape] at (0.35,8.2) {B. $\displaystyle\int_{C_1} \mathbf F\cdot d\mathbf r = f(1,0,2\pi) - f(1,0,0) =$};
\node[anchor=north west, font=\small\itshape] at (0.35,3.7) {C. $\displaystyle\int_{C_2} \mathbf F\cdot d\mathbf r =$ \quad because};
\end{tikzpicture}
</latex-image>
<shortdescription>Blank box in three labelled rows, for the gradient field, for the line integral along the helix by the fundamental theorem, and for the line integral along the segment with its justification.</shortdescription>
</image>
</sidebyside>
</statement>
</example>

<figure xml:id="fig-helix-segment-tikz">
<caption>
The two paths of <xref ref="example-ftli-helix"/> joining
<m>(1,0,0)</m> to <m>(1,0,2\pi)</m>: the helix <m>C_1</m>, parametrized
by <m>\mathbf r(t) = \cos(t)\mathbf i + \sin(t)\mathbf j + t\mathbf k</m>,
and the straight segment <m>C_2</m>.
</caption>
<image width="75%">
<shortdescription>
A helix and a vertical segment joining the same two points in space.
</shortdescription>
<description>
Three coordinate axes in space showing a blue helix that winds once
around the z axis, climbing from the point one comma zero comma zero up
to the point one comma zero comma two pi, and a vertical vermillion
segment joining the same two points directly. Both endpoints are
marked and labeled.
</description>
<latex-image>
<![CDATA[\begin{tikzpicture}[line join=round, line cap=round]
  % Okabe-Ito colorblind-safe palette
  \definecolor{oiblue}{RGB}{0,114,178}
  \definecolor{oivermillion}{RGB}{213,94,0}
  \definecolor{oigreen}{RGB}{0,158,115}
  \begin{axis}[
      view={115}{16},
      width=12.5cm,
      axis lines=center,
      axis line style={-latex, black!75, line width=0.7pt},
      xlabel={$x$}, ylabel={$y$}, zlabel={$z$},
      label style={font=\large},
      ticks=none,
      xmin=-1.4, xmax=1.5, ymin=-1.5, ymax=1.5, zmin=0, zmax=7.2,
      clip=false,
      legend style={
        at={(1.02,1.04)}, anchor=north east,
        font=\small,
        draw=black!35, rounded corners=2pt,
        fill=white, fill opacity=0.92, text opacity=1,
        inner sep=6pt, row sep=3pt,
      },
      legend cell align=left,
      legend image post style={line width=1.6pt, sharp plot},
    ]
    % ---- helix C1: (cos t, sin t, t), t in [0, 2pi] ----
    \addplot3[oiblue, line width=1.7pt,
        domain=0:360, samples=121, samples y=0]
      ({cos(x)}, {sin(x)}, {2*pi*x/360});
    \addlegendentry{$C_1$: helix $\mathbf r(t)=\cos t\,\mathbf i+\sin t\,\mathbf j+t\,\mathbf k$}
    % ---- segment C2 from (1,0,0) to (1,0,2pi) ----
    \addplot3[oivermillion, line width=1.7pt,
        samples y=0] coordinates {(1,0,0) (1,0,6.28318)};
    \addlegendentry{$C_2$: straight segment}
    % ---- direction arrowheads placed at the middle of each path ----
    \addplot3[oiblue, line width=1.7pt, -{Latex[length=2.4mm]},
        domain=168:180, samples=17, samples y=0, forget plot]
      ({cos(x)}, {sin(x)}, {2*pi*x/360});
    \addplot3[oivermillion, line width=1.7pt, -{Latex[length=2.4mm]},
        samples y=0, forget plot] coordinates {(1,0,2.85) (1,0,3.14159)};
    % ---- dashed guide circle in the xy-plane ----
    \addplot3[black!45, dashed, line width=1.2pt, domain=0:360,
        samples=97, samples y=0, forget plot]
      ({cos(x)}, {sin(x)}, 0);
    % ---- endpoints ----
    \addplot3[only marks, mark=*, mark size=1.9pt, color=black,
        forget plot] coordinates {(1,0,0) (1,0,6.28318)};
    \node[anchor=west, font=\small] at (axis cs:1.06,0,-0.35)
      {$(1,0,0)$};
    \node[oigreen, anchor=east, font=\small]
      at (axis cs:0.92,0,6.34) {$(1,0,2\pi)$};
  \end{axis}
\end{tikzpicture}]]>
</latex-image>
</image>
</figure>

</subsection>

<!-- ================================================================ -->
<subsection xml:id="sec-potential-function">
<title>Finding a Potential Function</title>

<example xml:id="example-potential-2d">
<title>Example II</title>
<statement>
<p>
Consider the vector field
<m>\mathbf F = (2xy + 3)\mathbf i + (x^2 - 4y)\mathbf j</m> in the
plane.
</p>

<p>
<ol marker="A.">
<li>
<p>
Find a function <m>f</m> such that <m>\mathbf F = \nabla f</m>.
</p>
</li>
<li>
<p>
Use the solution of part (A) to calculate the line integral of
<m>\mathbf F</m> along any curve from <m>(0,0)</m> to <m>(1,2)</m>.
</p>
</li>
</ol>
</p>
<p>
<em>Solution.</em>
(In <alert>A</alert>, match the two components of <m>\mathbf F</m> with
<m>\partial f/\partial x</m> and <m>\partial f/\partial y</m>, one at a
time. Step I: integrate the first with respect to <m>x</m>, remembering
that the <q>constant</q> of integration may depend on <m>y</m>. Step II:
differentiate what you have with respect to <m>y</m>, compare with the
second component, and solve for that function of <m>y</m>. In
<alert>B</alert>, say why <xref ref="thm-ftli"/> applies to
<em>any</em> curve from <m>(0,0)</m> to <m>(1,2)</m>, then evaluate. What
happens to the arbitrary constant?)
</p>

<sidebyside width="97%">
<image>
<latex-image>
\begin{tikzpicture}
\draw[rounded corners=8pt, draw=gray!55, thick] (0,0) rectangle (15,15);
\draw[gray!35] (0,11) -- (15,11);
\draw[gray!35] (0,6.5) -- (15,6.5);
\draw[gray!35] (0,3.5) -- (15,3.5);
\node[anchor=north west, font=\small\itshape] at (0.35,14.7) {Step I: $\dfrac{\partial f}{\partial x} = 2xy+3$, so $f(x,y) =$};
\node[anchor=north west, font=\small\itshape] at (0.35,10.7) {Step II: $\dfrac{\partial f}{\partial y} = x^2 - 4y$, so};
\node[anchor=north west, font=\small\itshape] at (0.35,6.2) {A. $f(x,y) =$};
\node[anchor=north west, font=\small\itshape] at (0.35,3.2) {B. $\displaystyle\int_C \mathbf F\cdot d\mathbf r = f(1,2) - f(0,0) =$};
\end{tikzpicture}
</latex-image>
<shortdescription>Blank box in four rows, for integrating the first component with respect to x, for matching the second component to determine the function of y, for the potential function, and for the line integral by the fundamental theorem.</shortdescription>
</image>
</sidebyside>
</statement>
</example>

<example xml:id="example-potential-function">
<title>Example III</title>
<statement>
<p>
Consider the vector field
<m>\mathbf F = yz\,\mathbf i + xz\,\mathbf j + (xy + 2z)\mathbf k</m>.
</p>

<p>
<ol marker="A.">
<li>
<p>
Find a function <m>f</m> such that <m>\mathbf F = \nabla f</m>.
</p>
</li>
<li>
<p>
Use the solution of part (A) to calculate the line integral from
<m>(1,0,-2)</m> to <m>(4,6,3)</m>.
</p>
</li>
</ol>
</p>
<p>
<em>Solution.</em>
(In <alert>A</alert>, match the three components one at a time. Step I:
integrate the first with respect to <m>x</m>; the constant of
integration may now depend on <m>y</m> and <m>z</m>. Step II:
differentiate with respect to <m>y</m> and compare with the second
component; what does that say about the dependence on <m>y</m>? Step
III: differentiate with respect to <m>z</m>, compare with the third
component, and solve. In <alert>B</alert>, apply
<xref ref="thm-ftli"/> with <m>A(1,0,-2)</m> and <m>B(4,6,3)</m>.)
</p>

<sidebyside width="97%">
<image>
<latex-image>
\begin{tikzpicture}
\draw[rounded corners=8pt, draw=gray!55, thick] (0,0) rectangle (15,17);
\draw[gray!35] (0,13.5) -- (15,13.5);
\draw[gray!35] (0,10) -- (15,10);
\draw[gray!35] (0,6.5) -- (15,6.5);
\draw[gray!35] (0,3.5) -- (15,3.5);
\node[anchor=north west, font=\small\itshape] at (0.35,16.7) {Step I: $\dfrac{\partial f}{\partial x} = yz$, so $f(x,y,z) =$};
\node[anchor=north west, font=\small\itshape] at (0.35,13.2) {Step II: $\dfrac{\partial f}{\partial y} = xz$, so};
\node[anchor=north west, font=\small\itshape] at (0.35,9.7) {Step III: $\dfrac{\partial f}{\partial z} = xy + 2z$, so};
\node[anchor=north west, font=\small\itshape] at (0.35,6.2) {A. $f(x,y,z) =$};
\node[anchor=north west, font=\small\itshape] at (0.35,3.2) {B. $\displaystyle\int_C \mathbf F\cdot d\mathbf r = f(4,6,3) - f(1,0,-2) =$};
\end{tikzpicture}
</latex-image>
<shortdescription>Blank box in five rows, for the three steps that match the components of the field to the partial derivatives of f, for the resulting potential function, and for the line integral by the fundamental theorem.</shortdescription>
</image>
</sidebyside>
</statement>
</example>

<!-- Example 1.4.10 revisited: the same work integral, but now with a    -->
<!-- potential function and the Fundamental Theorem instead of a         -->
<!-- parametrization.                                                    -->
<example xml:id="example-gravity-ftli">
<title>Example IV</title>
<statement>
<p>
In <xref ref="ex-helix-gravity"/> we computed the work done by the
inverse-square field
</p>

<me>
\mathbf F = \frac{\langle -x,\,-y,\,-z\rangle}
                 {\left(x^2+y^2+z^2\right)^{3/2}}
          = -\frac{\mathbf r}{\lVert\mathbf r\rVert^{3}}
</me>

<p>
along the helix <m>\mathbf r(t) = \langle \cos t,\ \sin t,\ t\rangle</m>
from <m>(1,0,0)</m> to <m>(1,0,2\pi)</m>, by parametrizing the curve.
Do it again with <xref ref="thm-ftli"/>.
</p>
<p>
<em>Solution.</em>
(First say why looking for a potential function is legitimate here:
where is the field defined, and does the helix avoid that point? Rather
than run the three steps of <xref ref="example-potential-function"/>,
notice that <m>\mathbf F</m> depends only on the distance
<m>\rho = \lVert\mathbf r\rVert = \sqrt{x^2+y^2+z^2}</m>, and that
<m>\partial\rho/\partial x = x/\rho</m>, and likewise for <m>y</m> and
<m>z</m>. Guess <m>f = 1/\rho</m> and check <m>\nabla f</m> with the
chain rule. Then the work needs nothing but <m>\rho</m> at the two
endpoints. Compare with <xref ref="ex-helix-gravity"/>, and say what
this calculation never used.)
</p>

<sidebyside width="97%">
<image>
<latex-image>
\begin{tikzpicture}
\draw[rounded corners=8pt, draw=gray!55, thick] (0,0) rectangle (15,14);
\draw[gray!35] (0,11) -- (15,11);
\draw[gray!35] (0,6.5) -- (15,6.5);
\draw[gray!35] (0,3) -- (15,3);
\node[anchor=north west, font=\small\itshape] at (0.35,13.7) {why a potential function may exist:};
\node[anchor=north west, font=\small\itshape] at (0.35,10.7) {for $f = 1/\rho$: \quad $\nabla f =$};
\node[anchor=north west, font=\small\itshape] at (0.35,6.2) {$\rho$ at $A$ and at $B$; \quad $\displaystyle\int_C \mathbf F\cdot d\mathbf r = f(B) - f(A) =$};
\node[anchor=north west, font=\small\itshape] at (0.35,2.7) {what was never used, and what that means for other paths:};
\end{tikzpicture}
</latex-image>
<shortdescription>Blank box in four rows, for the justification that a potential exists, for the gradient of one over rho, for the work from the endpoint values, and for the remark about path independence.</shortdescription>
</image>
</sidebyside>
</statement>
</example>

</subsection>

<!-- ================================================================ -->
<subsection xml:id="sec-conservative-gradient">
<title>Conservative Fields are Gradient Fields</title>

<theorem xml:id="thm-conservative-gradient">
<title>Conservative Fields are Gradient Fields</title>
<statement>
<p>
Let <m>\mathbf F = M\mathbf i + N\mathbf j + P\mathbf k</m> be a vector
field whose components are continuous throughout an open connected
region <m>D</m> in space (<xref ref="def-conservative-concepts"/>).
Then <m>\mathbf F</m> is conservative if and
only if <m>\mathbf F</m> is a gradient field <m>\nabla f</m> for a
differentiable function <m>f</m>.
</p>
</statement>
<proof>
<p>
(<alert>Gradient implies conservative</alert>: assume
<m>\mathbf F = \nabla f</m> and apply <xref ref="thm-ftli"/> to any
piecewise smooth curve from <m>A</m> to <m>B</m>; what does the answer
depend on? <alert>Conservative implies gradient</alert>: fix a point
<m>A_0</m> of <m>D</m> and <em>define</em> <m>f(x,y,z)</m> as the line
integral of <m>\mathbf F</m> from <m>A_0</m> to <m>(x,y,z)</m> along any
curve in <m>D</m>; say why connectedness and path independence make this
definition meaningful. To show <m>\partial f/\partial x = M</m>, compare
<m>f(x+h,y,z)</m> with <m>f(x,y,z)</m> by travelling along the straight
segment <m>L_h</m> from <m>(x,y,z)</m> to <m>(x+h,y,z)</m>, which lies
in <m>D</m> because <m>D</m> is open; parametrize <m>L_h</m>, so that
the difference quotient becomes an average of <m>M</m> over the segment,
and let <m>h \to 0</m> using the continuity of <m>M</m>. The other two
partials are the same argument.)
</p>

<sidebyside width="97%">
<image>
<latex-image>
\begin{tikzpicture}
\draw[rounded corners=8pt, draw=gray!55, thick] (0,0) rectangle (15,18);
\draw[gray!35] (0,14.5) -- (15,14.5);
\draw[gray!35] (0,11) -- (15,11);
\draw[gray!35] (0,7) -- (15,7);
\draw[gray!35] (0,3) -- (15,3);
\node[anchor=north west, font=\small\itshape] at (0.35,17.7) {gradient $\Rightarrow$ conservative:};
\node[anchor=north west, font=\small\itshape] at (0.35,14.2) {conservative $\Rightarrow$ gradient: define $f(x,y,z) =$ \quad (why this makes sense)};
\node[anchor=north west, font=\small\itshape] at (0.35,10.7) {$f(x+h,y,z) - f(x,y,z) = \displaystyle\int_{L_h} \mathbf F\cdot d\mathbf r =$ \quad (parametrize $L_h$)};
\node[anchor=north west, font=\small\itshape] at (0.35,6.7) {$\dfrac{f(x+h,y,z) - f(x,y,z)}{h} =$ \qquad $\xrightarrow{\,h\to 0\,}$};
\node[anchor=north west, font=\small\itshape] at (0.35,2.7) {the other two partials, and why $f$ is differentiable:};
\end{tikzpicture}
</latex-image>
<shortdescription>Blank box in five rows, for the easy direction of the proof, for the definition of the potential function as a line integral, for the difference of its values along a short segment, for the difference quotient and its limit, and for the remaining partial derivatives.</shortdescription>
</image>
</sidebyside>
</proof>
</theorem>

<p>
Path independence means that all of the paths in
<xref ref="fig-path-independence-prefigure"/> give the same value of the
line integral: if <m>\mathbf F</m> is conservative, then
</p>

<men xml:id="eq-three-paths">
\int_{C_1} \mathbf F\cdot d\mathbf r
= \int_{C_2} \mathbf F\cdot d\mathbf r
= \int_{C_3} \mathbf F\cdot d\mathbf r
= f(B) - f(A).
</men>

<figure xml:id="fig-path-independence-prefigure">
<caption>
Three different paths <m>C_1</m>, <m>C_2</m>, and <m>C_3</m> joining
<m>A</m> to <m>B</m>. For a conservative field, all three line
integrals have the same value <m>f(B) - f(A)</m>.
</caption>
<image width="55%">
<shortdescription>
Three different curves joining the points A and B.
</shortdescription>
<prefigure xmlns="https://prefigure.org" label="prefigure-path-independence">
<diagram dimensions="(340,280)" margins="10">
<definition>c1(t) = (4*t - 1.6*t*(1-t), 3*t - 3.4*t*(1-t))</definition>
<definition>c2(t) = (4*t, 3*t)</definition>
<definition>c3(t) = (4*t - 3.4*t*(1-t), 3*t + 3.2*t*(1-t))</definition>
<coordinates bbox="(-1.3,-1.2,5.3,4.6)">
<parametric-curve at="pi-c1" function="c1" domain="(0,1)"
    stroke="#0072B2" thickness="3"/>
<parametric-curve at="pi-c2" function="c2" domain="(0,1)"
    stroke="#009E73" thickness="3"/>
<parametric-curve at="pi-c3" function="c3" domain="(0,1)"
    stroke="#D55E00" thickness="3"/>
<!-- The direction of travel is shown at the midpoint of each path,     -->
<!-- rather than at its end, where the three arrowheads crowded the     -->
<!-- point B.  Tail = midpoint - v/2, so each arrow straddles the       -->
<!-- middle of its curve, pointing along the tangent there.             -->
<vector at="pi-c1-arrow" v="(0.72,0.54)" tail="(1.24,0.38)"
    stroke="#0072B2" thickness="3"/>
<vector at="pi-c2-arrow" v="(0.72,0.54)" tail="(1.64,1.23)"
    stroke="#009E73" thickness="3"/>
<vector at="pi-c3-arrow" v="(0.72,0.54)" tail="(0.79,2.03)"
    stroke="#D55E00" thickness="3"/>
<point at="pi-A" p="(0,0)" fill="black" alignment="sw">
<m>A</m>
</point>
<point at="pi-B" p="(4,3)" fill="black" alignment="ne">
<m>B</m>
</point>
<label at="pi-c1-label" anchor="(2.95,1.25)" alignment="se"
    color="#0072B2"><m>C_1</m></label>
<label at="pi-c2-label" anchor="(3.15,2.15)" alignment="se"
    color="#009E73"><m>C_2</m></label>
<label at="pi-c3-label" anchor="(2.35,3.0)" alignment="nw"
    color="#D55E00"><m>C_3</m></label>
</coordinates>
<annotations>
<annotation ref="figure" text="Two points A and B in the plane joined by three different curves. C 1 bows below the straight path, C 2 is the straight segment from A to B, and C 3 bows above. An arrow at the middle of each curve points from A to B.">
  <annotation ref="pi-A" text="The starting point A of all three paths."/>
  <annotation ref="pi-B" text="The ending point B of all three paths."/>
  <annotation ref="pi-c1" text="The path C 1, drawn in blue, bowing below the straight segment, with an arrow at its midpoint pointing from A toward B."/>
  <annotation ref="pi-c2" text="The path C 2, the straight segment from A to B, drawn in green, with an arrow at its midpoint."/>
  <annotation ref="pi-c3" text="The path C 3, drawn in vermillion, bowing above the straight segment, with an arrow at its midpoint."/>
</annotation>
</annotations>
</diagram>
</prefigure>
</image>
</figure>

</subsection>

<!-- ================================================================ -->
<subsection xml:id="sec-loop-property">
<title>The Loop Property of Conservative Fields</title>

<theorem xml:id="thm-loop-property">
<title>Loop Property of Conservative Fields</title>
<statement>
<p>
The following statements are equivalent.
</p>

<p>
<ol>
<li>
<p>
<m>\oint_C \mathbf F\cdot d\mathbf r = 0</m> around every loop (that
is, closed curve <m>C</m>) in <m>D</m>.
</p>
</li>
<li>
<p>
The field <m>\mathbf F</m> is conservative on <m>D</m>.
</p>
</li>
</ol>
</p>
</statement>
<proof>
<p>
(<alert>(2) implies (1)</alert>: split any loop as
<m>C = C_1 \cup C_2</m>, with <m>C_1</m> from <m>A</m> to <m>B</m> and
<m>C_2</m> from <m>B</m> back to <m>A</m>, as in
<xref ref="fig-loop-split-prefigure"/>. Reversing <m>C_2</m> gives a
second path <m>-C_2</m> from <m>A</m> to <m>B</m>; what does path
independence say about <m>\int_{C_1}</m> and <m>\int_{-C_2}</m>, and
how is <m>\int_{-C_2}</m> related to <m>\int_{C_2}</m>? Add up.
<alert>(1) implies (2)</alert>: take any two paths <m>C_1</m>,
<m>C_2</m> from <m>A</m> to <m>B</m>, build a loop out of them, and
apply the assumption to it.)
</p>

<sidebyside width="97%">
<image>
<latex-image>
\begin{tikzpicture}
\draw[rounded corners=8pt, draw=gray!55, thick] (0,0) rectangle (15,12);
\draw[gray!35] (0,6) -- (15,6);
\node[anchor=north west, font=\small\itshape] at (0.35,11.7) {(2) $\Rightarrow$ (1): \quad $\displaystyle\oint_C \mathbf F\cdot d\mathbf r = \int_{C_1} \mathbf F\cdot d\mathbf r + \int_{C_2} \mathbf F\cdot d\mathbf r =$};
\node[anchor=north west, font=\small\itshape] at (0.35,5.7) {(1) $\Rightarrow$ (2): \quad the loop $C = C_1 \cup (-C_2)$ gives \quad $0 =$};
\end{tikzpicture}
</latex-image>
<shortdescription>Blank box in two rows, one for each direction of the proof of the loop property.</shortdescription>
</image>
</sidebyside>
</proof>
</theorem>

<figure xml:id="fig-loop-split-prefigure">
<caption>
On the left, a loop <m>C</m> is split into two pieces:
<m>C_1</m> from <m>A</m> to <m>B</m> and <m>C_2</m> from <m>B</m> back
to <m>A</m>, so <m>C = C_1 \cup C_2</m>. On the right, the orientation
of <m>C_2</m> is reversed to produce <m>-C_2</m>; now <m>C_1</m> and
<m>-C_2</m> both connect <m>A</m> to <m>B</m>, and path independence
forces the two integrals to agree.
</caption>
<sidebyside widths="48% 48%">
<image>
<shortdescription>
An oval loop through A and B split into an upper and a lower half.
</shortdescription>
<prefigure xmlns="https://prefigure.org" label="prefigure-loop-split">
<diagram dimensions="(300,260)" margins="10">
<definition>top(t) = (2*cos(t), 1.35*sin(t) + 0.12*sin(2*t))</definition>
<definition>bot(t) = (2*cos(t), 1.35*sin(t) - 0.1*sin(2*t))</definition>
<coordinates bbox="(-3.1,-2.4,3.1,2.4)">
<parametric-curve at="ls-c1" function="top" domain="(0,pi)"
    stroke="#0072B2" thickness="3" arrows="1"/>
<parametric-curve at="ls-c2" function="bot" domain="(pi,2*pi)"
    stroke="#CC79A7" thickness="3" arrows="1"/>
<point at="ls-A" p="(2,0)" fill="black" alignment="e">
<m>A</m>
</point>
<point at="ls-B" p="(-2,0)" fill="black" alignment="w">
<m>B</m>
</point>
<label at="ls-c1-label" anchor="(-0.9,1.55)" alignment="n"
    color="#0072B2"><m>C_1</m></label>
<label at="ls-c2-label" anchor="(1.75,-1.05)" alignment="se"
    color="#CC79A7"><m>C_2</m></label>
<label at="ls-eq" anchor="(0,-1.85)" alignment="s">
<m>C = C_1 \cup C_2</m>
</label>
</coordinates>
<annotations>
<annotation ref="figure" text="An oval loop through the points A on the right and B on the left. The upper half, labeled C 1, is drawn in blue and runs counterclockwise from A to B. The lower half, labeled C 2, is drawn in reddish purple and continues from B back to A, closing the loop.">
  <annotation ref="ls-c1" text="The piece C 1 of the loop, running from A to B along the top."/>
  <annotation ref="ls-c2" text="The piece C 2 of the loop, running from B back to A along the bottom, so the loop is the union of C 1 and C 2."/>
</annotation>
</annotations>
</diagram>
</prefigure>
</image>
<image>
<shortdescription>
The same oval with the lower half reversed so both halves run from A to B.
</shortdescription>
<prefigure xmlns="https://prefigure.org" label="prefigure-loop-reversed">
<diagram dimensions="(300,260)" margins="10">
<definition>top(t) = (2*cos(t), 1.35*sin(t) + 0.12*sin(2*t))</definition>
<definition>botrev(t) = (2*cos(-t), 1.35*sin(-t) + 0.1*sin(2*t))</definition>
<coordinates bbox="(-3.1,-2.4,3.1,2.4)">
<parametric-curve at="lr-c1" function="top" domain="(0,pi)"
    stroke="#0072B2" thickness="3" arrows="1"/>
<parametric-curve at="lr-negc2" function="botrev" domain="(0,pi)"
    stroke="#009E73" thickness="3" arrows="1"/>
<point at="lr-A" p="(2,0)" fill="black" alignment="e">
<m>A</m>
</point>
<point at="lr-B" p="(-2,0)" fill="black" alignment="w">
<m>B</m>
</point>
<label at="lr-c1-label" anchor="(-0.9,1.55)" alignment="n"
    color="#0072B2"><m>C_1</m></label>
<label at="lr-negc2-label" anchor="(1.75,-1.05)" alignment="se"
    color="#009E73"><m>-C_2</m></label>
<label at="lr-eq" anchor="(0,-1.85)" alignment="s">
<m>\int_{C_1}\mathbf F\cdot d\mathbf r
= \int_{-C_2}\mathbf F\cdot d\mathbf r</m>
</label>
</coordinates>
<annotations>
<annotation ref="figure" text="The same oval through A and B, but now both halves run from A to B. The upper half C 1 is drawn in blue and the lower half, relabeled minus C 2 and drawn in green, has its arrow reversed so that it also runs from A to B. Since both curves connect A to B, path independence makes the two line integrals equal.">
  <annotation ref="lr-c1" text="The piece C 1, still running from A to B along the top."/>
  <annotation ref="lr-negc2" text="The reversed piece minus C 2, drawn in green, now running from A to B along the bottom. C 1 and minus C 2 both connect A to B."/>
</annotation>
</annotations>
</diagram>
</prefigure>
</image>
</sidebyside>
</figure>

<p>
We can now collect everything this section has proved. The content of
<xref ref="thm-conservative-gradient"/>, combined with
<xref ref="thm-ftli"/>, the loop property in
<xref ref="thm-loop-property"/>, and the definition of path independence
in <xref ref="def-conservative-concepts"/>, can be organized into the
following square of equivalent statements. Throughout, <m>\mathbf F</m>
is a continuous field with continuous partial derivatives on an open,
connected, simply connected region <m>D</m>.
</p>

<figure xml:id="fig-equivalence-square-prefigure">
<caption>
The four statements are equivalent. Each double arrow joins the middle of
one box to the middle of the next, and may be read in either direction.
</caption>
<image width="100%">
<shortdescription>
Four boxed statements at the corners of a square, joined by double arrows.
</shortdescription>
<prefigure xmlns="https://prefigure.org" label="prefigure-equivalence-square">
<diagram dimensions="(580,271)" margins="6">
<coordinates bbox="(0,0,12,5.6)">
<!-- Boxes.  Left column centred on x = 2.5, right on x = 9.0; the top   -->
<!-- row centred on y = 4.45, the bottom row on y = 1.45.  Every arrow   -->
<!-- below runs between two of those centres, so it meets each box at    -->
<!-- the middle of an edge.                                              -->
<rectangle at="eq-box-cons" lower-left="(0.2,3.9)" dimensions="(4.6,1.1)"
    stroke="#0072B2" fill="#dbeaf7" thickness="2"/>
<label at="eq-lbl-cons" anchor="(2.5,4.45)" alignment="c">
<m>\mathbf F \text{ is a conservative field}</m>
</label>

<rectangle at="eq-box-grad" lower-left="(6.4,3.6)" dimensions="(5.2,1.7)"
    stroke="#0072B2" fill="#dbeaf7" thickness="2"/>
<label at="eq-lbl-grad1" anchor="(9.0,4.82)" alignment="c">
<m>\mathbf F \text{ is a gradient field}</m>
</label>
<label at="eq-lbl-grad2" anchor="(9.0,4.08)" alignment="c">
<m>\mathbf F = \nabla f \text{ for some } f</m>
</label>

<rectangle at="eq-box-loop" lower-left="(0.2,0.6)" dimensions="(4.6,1.7)"
    stroke="#0072B2" fill="#dbeaf7" thickness="2"/>
<label at="eq-lbl-loop1" anchor="(2.5,1.82)" alignment="c">
<m>\oint_C \mathbf F\cdot d\mathbf r = 0</m>
</label>
<label at="eq-lbl-loop2" anchor="(2.5,1.08)" alignment="c">
<m>\text{on every loop}</m>
</label>

<rectangle at="eq-box-path" lower-left="(6.4,0.6)" dimensions="(5.2,1.7)"
    stroke="#0072B2" fill="#dbeaf7" thickness="2"/>
<label at="eq-lbl-path1" anchor="(9.0,1.82)" alignment="c">
<m>\int_C \mathbf F\cdot d\mathbf r \text{ is path independent}</m>
</label>
<label at="eq-lbl-path2" anchor="(9.0,1.08)" alignment="c">
<m>\int_C \mathbf F\cdot d\mathbf r = f(B) - f(A)</m>
</label>

<!-- Double arrows, centre to centre. -->
<line at="eq-arrow-top" endpoints="((4.8,4.45),(6.4,4.45))"
    stroke="#333333" thickness="2" arrows="2"/>
<line at="eq-arrow-bottom" endpoints="((4.8,1.45),(6.4,1.45))"
    stroke="#333333" thickness="2" arrows="2"/>
<line at="eq-arrow-left" endpoints="((2.5,3.9),(2.5,2.3))"
    stroke="#333333" thickness="2" arrows="2"/>
<line at="eq-arrow-right" endpoints="((9.0,3.6),(9.0,2.3))"
    stroke="#333333" thickness="2" arrows="2"/>
</coordinates>

<annotations>
<annotation ref="figure" text="Four boxed statements at the corners of a square, each joined to its neighbours by a double-headed arrow running from the middle of one box to the middle of the next. Top left: F is a conservative field. Top right: F is a gradient field, that is F equals grad f for some f. Bottom left: the loop integral of F dot d r is zero on every loop. Bottom right: the integral of F dot d r is path independent and equals f of B minus f of A. Because every arrow is double-headed, any one of the four statements implies all of the others.">
  <annotation ref="eq-box-cons" text="Top left box: F is a conservative field."/>
  <annotation ref="eq-box-grad" text="Top right box: F is a gradient field, F equals grad f for some potential function f."/>
  <annotation ref="eq-box-loop" text="Bottom left box: the integral of F dot d r around every loop is zero."/>
  <annotation ref="eq-box-path" text="Bottom right box: the integral of F dot d r is path independent, and equals f of B minus f of A."/>
  <annotation ref="eq-arrow-top" text="A double arrow across the top: conservative if and only if gradient field."/>
  <annotation ref="eq-arrow-bottom" text="A double arrow across the bottom: the loop property if and only if path independence."/>
  <annotation ref="eq-arrow-left" text="A double arrow down the left side: conservative if and only if the loop property."/>
  <annotation ref="eq-arrow-right" text="A double arrow down the right side: gradient field if and only if path independence."/>
</annotation>
</annotations>
</diagram>
</prefigure>
</image>
</figure>

<p>
Read across the top: a conservative field is exactly a gradient field,
and the potential function <m>f</m> is what makes that concrete. Read
across the bottom: path independence and the loop property are two ways
of saying the same thing, and the Fundamental Theorem turns either one
into a number, <m>f(B) - f(A)</m>. Read down either side: each statement
on the top implies, and is implied by, the one beneath it. In practice
the four are used in whichever direction is convenient<mdash/>find a
potential function to evaluate a line integral, or observe that a field
is a gradient field to conclude at once that its integral around every
loop vanishes.
</p>

</subsection>

<!-- ==================================================================== -->
<!-- Questions 4-6 of the companion GeoGebra activity, with the three      -->
<!-- applets VF-fig 2, VF-fig 3 and VF-fig 4 (600 x 500).                  -->
<subsection xml:id="sec-conservative-concept-check">
<title>Concept Check</title>

<p>
The three questions below all use the applets that go with them. In each
one, drag the slider to send the point along the curve and watch the field
vector and the tangent vector move with it; the sign of their dot product is
what the integral is accumulating. Every applet can be rotated by dragging
the background and zoomed by scrolling.
</p>

<exercise xml:id="q-vf-helix-work">
<title>Work Along the Helix, Two Ways</title>
<statement>
<p>
For the field and path of <xref ref="fig-vf-helix-work"/>:
</p>
<ol>
<li>
<p>
Use the parametrized path to compute the work done by the force field from
point <m>A</m> to point <m>B</m>.
</p>
</li>
<li>
<p>
Is it possible to use the fundamental theorem of line integrals
<xref ref="eq-ftli"/> to compute that work? If so, use it to verify your
answer to the previous part. Justify your answer fully.
</p>
</li>
</ol>
<p>
(Read the parametrization and the field off
<xref ref="fig-vf-helix-work"/>, and find the parameter values of
<m>A</m> and <m>B</m>. For part 1, write <m>\mathbf F(\mathbf r(t))</m>
and <m>\mathbf v(t)</m>, dot them, and integrate. For part 2, decide
whether <m>\mathbf F</m> is conservative <mdash/> where is it defined,
and can you find a potential function? <mdash/> and if so evaluate
<m>f(B) - f(A)</m>. Then explain, from the picture, why the two answers
agree.)
</p>

<sidebyside width="97%">
<image>
<latex-image>
\begin{tikzpicture}
\draw[rounded corners=8pt, draw=gray!55, thick] (0,0) rectangle (15,15);
\draw[gray!35] (0,12.5) -- (15,12.5);
\draw[gray!35] (0,7.5) -- (15,7.5);
\draw[gray!35] (0,3) -- (15,3);
\node[anchor=north west, font=\small\itshape] at (0.35,14.7) {$A = \mathbf r(\;\;)$, $B = \mathbf r(\;\;)$};
\node[anchor=north west, font=\small\itshape] at (0.35,12.2) {1. $\mathbf F(\mathbf r(t)) =$ \quad $\mathbf v(t) =$ \quad $\mathbf F\cdot\mathbf v =$ \quad $W =$};
\node[anchor=north west, font=\small\itshape] at (0.35,7.2) {2. is $\mathbf F$ conservative? \quad potential $f =$ \quad $W = f(B) - f(A) =$};
\node[anchor=north west, font=\small\itshape] at (0.35,2.7) {why the two agree, from the picture:};
\end{tikzpicture}
</latex-image>
<shortdescription>Blank box in four rows, for the parameter values of the endpoints, for the direct computation of the work, for the fundamental-theorem computation with its justification, and for the geometric explanation.</shortdescription>
</image>
</sidebyside>
</statement>
</exercise>

<figure xml:id="fig-vf-helix-work">
<caption>
A point climbing the helix
<m>\mathbf{r}(t)=\left\langle \cos t,\ \sin t,\ t/2\right\rangle</m> from
<m>A</m> to <m>B</m>, through the force field
<m>\mathbf{F}=\left\langle -x,\ y,\ 0\right\rangle</m>. The two arrows are
<m>\mathbf{F}</m> at the moving point and the velocity <m>\mathbf{v}</m>.
</caption>
<interactive geogebra="arjpdtkt"
             width="100%" aspect="600:500"
             material-width="600" material-height="500"
             shift-drag-zoom="yes" reset-icon="yes">
<instructions>
<p>
Drag the slider <m>t</m> to move the point from <m>A</m> to <m>B</m> along
the helix. At each position the applet draws the force
<m>\mathbf{F}=\left\langle -x,y,0\right\rangle</m> acting there and the
velocity <m>\mathbf{v}=d\mathbf{r}/dt</m>. Watch the angle between them: it
is acute on some stretches and obtuse on others, so the work is being added
on some parts of the trip and taken back on others.
</p>
</instructions>
</interactive>
</figure>

<exercise xml:id="q-vf-segment">
<title>A Line Integral Along a Segment</title>
<statement>
<p>
Compute the line integral of the vector field along the line segment shown
in <xref ref="fig-vf-segment"/>.
</p>
<p>
(Read the field and the endpoints off <xref ref="fig-vf-segment"/>.
Before parametrizing anything, test whether the field is conservative,
and look for a potential function by inspection. If you find one, the
segment never needs to be parametrized.)
</p>

<sidebyside width="97%">
<image>
<latex-image>
\begin{tikzpicture}
\draw[rounded corners=8pt, draw=gray!55, thick] (0,0) rectangle (15,10);
\draw[gray!35] (0,6.5) -- (15,6.5);
\draw[gray!35] (0,3.5) -- (15,3.5);
\node[anchor=north west, font=\small\itshape] at (0.35,9.7) {is $\mathbf F$ conservative?};
\node[anchor=north west, font=\small\itshape] at (0.35,6.2) {potential: $f(x,y,z) =$ \qquad check $\nabla f =$};
\node[anchor=north west, font=\small\itshape] at (0.35,3.2) {$\displaystyle\int_C \mathbf F\cdot d\mathbf r = f(B) - f(A) =$};
\end{tikzpicture}
</latex-image>
<shortdescription>Blank box in three rows, for testing whether the field is conservative, for a potential function and its check, and for the line integral from the endpoint values.</shortdescription>
</image>
</sidebyside>
</statement>
</exercise>

<figure xml:id="fig-vf-segment">
<caption>
The straight segment from <m>A=(0,1,-1)</m> to <m>B=(1,2\sqrt2,2)</m> in the
field <m>\mathbf{F}=\tfrac13\left\langle z,\ z,\ x+y\right\rangle</m>, with
<m>\mathbf{F}</m> and the unit tangent <m>\hat{\mathbf{T}}</m> drawn at the
moving point.
</caption>
<!-- Served from a local copy of the material whose stored window is
     458 + 142 = 600 wide by 500 high, so the narrow panel carrying the
     slider is part of the layout.  (The published material stores a
     1098 + 142 window, which loses the panel when squeezed to 600.) -->
<interactive platform="geogebra" width="100%" aspect="600:500">
<slate surface="geogebra" source="geogebra/vf-fig3-segment.ggb"
       aspect="600:500" material-width="600" material-height="500"/>
<instructions>
<p>
Drag the slider to move the point along the segment from <m>A</m> to
<m>B</m>. The applet draws the field vector <m>\mathbf{F}</m> and the unit
tangent <m>\hat{\mathbf{T}}</m> at that point, so what the integral
accumulates is the length of the projection of <m>\mathbf{F}</m> onto
<m>\hat{\mathbf{T}}</m>. Rotate the view to see how the segment sits
relative to the field.
</p>
</instructions>
</interactive>
</figure>

<exercise xml:id="q-vf-curve">
<title>The Same Endpoints, a Wilder Path</title>
<statement>
<p>
Compute the line integral of the vector field from point <m>A</m> to point
<m>B</m> as shown in <xref ref="fig-vf-curve"/>, where the curve is
parametrized as
</p>

<md>
\mathbf{r}(t)=\left\langle \frac{\ln(t)}{\ln(2)},\ t^{3/2},\
t\cos(\pi t)\right\rangle .
</md>
<p>
(First check the endpoints: evaluate <m>\mathbf r(1)</m> and
<m>\mathbf r(2)</m> and compare with <xref ref="fig-vf-segment"/>. Then
decide whether you need to differentiate <m>t\cos(\pi t)</m> at all, and
say why. Compare your answer with <xref ref="q-vf-segment"/>.)
</p>

<sidebyside width="97%">
<image>
<latex-image>
\begin{tikzpicture}
\draw[rounded corners=8pt, draw=gray!55, thick] (0,0) rectangle (15,8);
\draw[gray!35] (0,4.5) -- (15,4.5);
\node[anchor=north west, font=\small\itshape] at (0.35,7.7) {$\mathbf r(1) =$ \qquad $\mathbf r(2) =$};
\node[anchor=north west, font=\small\itshape] at (0.35,4.2) {$\displaystyle\int_C \mathbf F\cdot d\mathbf r =$ \qquad because};
\end{tikzpicture}
</latex-image>
<shortdescription>Blank box in two rows, for the two endpoints of the curve and for the value of the line integral with its justification.</shortdescription>
</image>
</sidebyside>
</statement>
</exercise>

<figure xml:id="fig-vf-curve">
<caption>
The same field <m>\mathbf{F}=\tfrac13\left\langle z,\ z,\ x+y\right\rangle</m>
and the same endpoints as in <xref ref="fig-vf-segment"/>, but now joined by
the curve
<m>\mathbf{r}(t)=\left\langle \frac{\ln t}{\ln 2},\ t^{3/2},\
t\cos(\pi t)\right\rangle</m>.
</caption>
<!-- as above: a local copy sized 459 + 141 by 500 -->
<interactive platform="geogebra" width="100%" aspect="600:500">
<slate surface="geogebra" source="geogebra/vf-fig4-curve.ggb"
       aspect="600:500" material-width="600" material-height="500"/>
<instructions>
<p>
Drag the slider to move the point along the curve from <m>A</m> to
<m>B</m>, watching <m>\mathbf{F}</m> and the unit tangent
<m>\hat{\mathbf{T}}</m> as before. Compare the shape of this path with the
segment of <xref ref="fig-vf-segment"/>: the two share their endpoints and
nothing else.
</p>
</instructions>
</interactive>
</figure>

</subsection>

</section>
