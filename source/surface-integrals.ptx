<?xml version="1.0" encoding="utf-8"?>
<!-- MATH 14 Skeletal Notes                                                -->
<!-- Section: Surface Integrals: Flux Through a Surface                    -->
<!--                                                                       -->
<!-- Skeletal (fill-in) version of the section of the same name in the     -->
<!-- completed MATH 14 notes.  The construction of the tangent vectors     -->
<!-- and the normal to a patch, the definition of the surface integral and -->
<!-- the parametrized flux formula are kept; the flat-plate flow rate, the -->
<!-- five worked examples and the sphere's normal are replaced by blank    -->
<!-- workspace boxes, each preceded by a one-line instruction.  The        -->
<!-- sphere's cross product and unit normal are restated in an assemblage  -->
<!-- so that Stokes' theorem can still cite eq-sphere-cross and            -->
<!-- eq-sphere-unit-normal.  File name and xml:ids are unchanged.          -->
<!--                                                                       -->
<!-- Figures: all kept (the tilted plate, the sphere patch, the cylinder   -->
<!-- normals, the paraboloid-and-disk surface, and every video); each is   -->
<!-- the check on the box above it.  Captions that stated a formula or a   -->
<!-- numerical answer have been trimmed.                                   -->
<section xml:id="section-surface-integrals" xmlns:xi="http://www.w3.org/2001/XInclude">
<title>Surface Integrals: Flux Through a Surface</title>

<introduction>
<p>
How much fluid crosses a surface per unit time? For a flat surface in a
constant velocity field the answer is a single dot product; for a curved
surface in a varying field it is a <term>surface integral</term>. In
this section we build the surface integral in three steps: we learn how
to parametrize a surface, how to use the parametrization to find a
normal vector, and how to combine the two to compute the flux of a
vector field.
</p>
</introduction>

<!-- ================================================================ -->
<subsection xml:id="sec-flux-flat">
<title>Flux Through a Flat Surface</title>

<p>
Suppose water flows with constant velocity <m>\mathbf{v}_0</m> through
a flat surface <m>S</m> of area <m>A</m> that is perpendicular to the
flow. How much water passes through <m>S</m> in one second, and in what
units? Now tilt the surface, so that the flow meets it at an angle
<m>\theta</m> away from the normal direction, as in
<xref ref="fig-flux-plate"/>: only the component of the velocity
perpendicular to the surface pushes water through it. Write the flow
rate, and then rewrite it as a dot product of <m>\mathbf{v}_0</m> with
a normal vector <m>\mathbf{n} = \hat{\mathbf{n}}\,A</m> whose length is
the area of <m>S</m>.
</p>

<sidebyside width="97%">
<image>
<latex-image>
\begin{tikzpicture}
\draw[rounded corners=8pt, draw=gray!55, thick] (0,0) rectangle (15,8);
\draw[gray!35] (0,5.5) -- (15,5.5);
\draw[gray!35] (0,3) -- (15,3);
\node[anchor=north west, font=\small\itshape] at (0.35,7.7) {perpendicular flow: flow rate $=$ \qquad units:};
\node[anchor=north west, font=\small\itshape] at (0.35,5.2) {tilted by $\theta$: flow rate $=$};
\node[anchor=north west, font=\small\itshape] at (0.35,2.7) {as a dot product: flow rate $=$};
\end{tikzpicture}
</latex-image>
<shortdescription>Blank box in three rows, for the flow rate through a perpendicular plate with its units, for the flow rate through a tilted plate, and for the same quantity written as a dot product with the normal vector.</shortdescription>
</image>
</sidebyside>

<figure xml:id="fig-flux-plate">
<caption>
A tilted plate <m>S</m> of area <m>A</m> in a uniform flow
<m>\mathbf{v}_0</m>. Only the component of <m>\mathbf{v}_0</m> along
the normal <m>\mathbf{n}</m> carries fluid through the plate.
</caption>
<image xml:id="image-flux-plate" width="70%">
<shortdescription>
A tilted plate in a uniform flow, with its normal vector and the angle
between the normal and the velocity marked.
</shortdescription>
<description>
<p>
A blue parallelogram-shaped plate <m>S</m> stands tilted in a uniform
flow of parallel gray velocity arrows <m>\mathbf{v}_0</m> pointing up
and to the right. A magenta arrow <m>\mathbf{n}</m>, normal to the
plate, makes an angle <m>\theta</m> with the velocity arrows.
</p>
</description>
<latex-image>
<![CDATA[\begin{tikzpicture}[line join=round, line cap=round]
  \tikzset{lbl/.style={fill=white, fill opacity=0.82, text opacity=1, inner sep=2.5pt, rounded corners=2pt}}
  \definecolor{oiblue}{RGB}{0,114,178}
  \definecolor{oimagenta}{RGB}{204,0,119}
  \begin{axis}[
      view={115}{18},
      width=11.5cm,
      axis lines=none,
      axis equal image,
      xmin=-2.2, xmax=2.6, ymin=-2.6, ymax=3.2, zmin=-1.4, zmax=2.6,
      clip=false,
    ]
    % uniform flow arrows (direction (0,1,0.35), normalized-ish)
    \foreach \xx/\zz in {-1.4/-0.9, -1.4/0.1, -1.4/1.1, 0/-0.9, 0/0.1,
                         0/1.1, 1.4/-0.9, 1.4/0.1, 1.4/1.1}{
      \edef\tmp{\noexpand\draw[-latex, black!35, line width=0.9pt]
        (axis cs:\zz,-2.2,\xx) -- (axis cs:\zz,-0.9,{\xx+0.45});}\tmp
      \edef\tmp{\noexpand\draw[-latex, black!35, line width=0.9pt]
        (axis cs:\zz,0.6,{\xx+0.55}) -- (axis cs:\zz,1.9,{\xx+1.0});}\tmp}
    % the plate: a parallelogram with normal in the y-z plane, tilted
    % plate spanned by u=(1,0,0) and w=(0,0.5,0.9)(scaled)
    \fill[oiblue!35, draw=oiblue, line width=1.2pt, fill opacity=0.75]
      (axis cs:-1.1,-0.7,0.6) -- (axis cs:1.1,-0.7,0.6)
      -- (axis cs:1.1,0.7,-0.6) -- (axis cs:-1.1,0.7,-0.6) -- cycle;
    \node[lbl, font=\large, text=oiblue] at (axis cs:-0.7,-0.35,0.35) {$S$};
    % normal vector at the plate center (n ~ (0, 0.65, 0.76))
    \draw[-latex, oimagenta, line width=1.6pt]
      (axis cs:0,0,0) -- (axis cs:0,0.85,0.99);
    \node[lbl, font=\normalsize, text=oimagenta, anchor=south]
      at (axis cs:0,0.9,1.05) {$\mathbf{n} = \hat{\mathbf{n}}\,A$};
    % one bold velocity arrow from plate center (direction (0,1,0.35))
    \draw[-latex, black!75, line width=1.6pt]
      (axis cs:0,0,0) -- (axis cs:0,1.3,0.455);
    \node[lbl, font=\normalsize, anchor=west]
      at (axis cs:0,1.35,0.45) {$\mathbf{v}_0$};
    % angle arc between n and v0 (both start at plate center)
    \draw[black!60, line width=0.8pt]
      (axis cs:0,0.36,0.42) to[bend left=25] (axis cs:0,0.55,0.19);
    \node[lbl, font=\small] at (axis cs:0,0.58,0.42) {$\theta$};
  \end{axis}
\end{tikzpicture}]]>
</latex-image>
</image>
</figure>

<p>
Our goal is to generalize the above scenario in two respects: (I) we
generalize the surface <m>S</m> (not just a plane), and (II) we
generalize the vector field (not just a constant). To achieve this
goal, we will need to learn: (I) how to parametrize a surface
<m>S</m>; (II) how to use the parametrization of <m>S</m> to find the
normal vector <m>\mathbf{n}</m>; and (III) how to use (I) and (II) to
compute the flux of a vector field. Watch
<xref ref="fig-flux-plate-video"/> for the flat-plate picture.
</p>

<figure xml:id="fig-flux-plate-video">
<caption>
Flow through a flat plate: perpendicular flow, and then the plate
tilted by an angle <m>\theta</m>. Use it to check the flow rate you
wrote down.
</caption>
<video preview="play-button.png" xml:id="video-flux-plate"
       source="videos/flux_flat_plate_1080p.mp4"
       width="70%" aspect="16:9"/>
</figure>

</subsection>

<!-- ================================================================ -->
<subsection xml:id="sec-parametric-surfaces">
<title>Parametric Surfaces</title>

<p>
Recall how we parametrize curves in the <m>xy</m>-plane: the parabola
<m>y = x^2</m> is traced by
<m>\mathbf{r}(t) = \langle t, t^2 \rangle</m> for <m>t \in \R</m>;
only one parameter is needed. A surface is two-dimensional, so its
parametrization needs <em>two</em> parameters.
</p>

<example xml:id="example-parametric-surfaces">
<title>Parametrizing Surfaces</title>
<statement>
<p>
Find parametric representations for each of the following surfaces.
</p>

<p>
<ol marker="A.">
<li>
<p>
<m>z = x^2 + y^2</m>
</p>
</li>
<li>
<p>
<m>x^2 + y^2 + z^2 = 4</m>
</p>
</li>
<li>
<p>
<m>x^2 + y^2 = 1</m>
</p>
</li>
</ol>
</p>
<p>
<em>Solution.</em>
(In <alert>A</alert>, start from the position vector
<m>\langle x, y, z\rangle</m> of an arbitrary point and impose the
constraint, so that <m>x</m> and <m>y</m> themselves are the
parameters; then do the same in cylindrical and in spherical
coordinates, stating the ranges of the parameters. In <alert>B</alert>,
what surface is this, and which coordinate system fits it? In
<alert>C</alert>, one parameter goes around and one is free. Compare
with <xref ref="fig-parametric-surfaces-video"/>.)
</p>

<sidebyside width="97%">
<image>
<latex-image>
\begin{tikzpicture}
\draw[rounded corners=8pt, draw=gray!55, thick] (0,0) rectangle (15,15);
\draw[gray!35] (0,7.5) -- (15,7.5);
\draw[gray!35] (0,4) -- (15,4);
\node[anchor=north west, font=\small\itshape] at (0.35,14.7) {A. $\mathbf r(x,y) =$ \qquad $\mathbf r(r,\theta) =$ \qquad $\mathbf r(\theta,\phi) =$};
\node[anchor=north west, font=\small\itshape] at (0.35,7.2) {B. $\mathbf r(\theta,\phi) =$};
\node[anchor=north west, font=\small\itshape] at (0.35,3.7) {C. $\mathbf r(\theta,z) =$};
\end{tikzpicture}
</latex-image>
<shortdescription>Blank box in three labelled rows, for the parametrizations of the paraboloid in three coordinate systems, of the sphere, and of the cylinder, each with the ranges of its parameters.</shortdescription>
</image>
</sidebyside>
</statement>
</example>

<figure xml:id="fig-parametric-surfaces-video">
<caption>
Parametrizing the paraboloid <m>z = x^2 + y^2</m>, the sphere of
radius <m>2</m>, and the cylinder <m>x^2 + y^2 = 1</m>: two parameters
sweep out coordinate grids on each surface.
</caption>
<video preview="play-button.png" xml:id="video-parametric-surfaces"
       source="videos/parametric_surfaces_1080p.mp4"
       width="70%" aspect="16:9"/>
</figure>

</subsection>

<!-- ================================================================ -->
<subsection xml:id="sec-surface-normal">
<title>The Normal Vector to a Surface</title>

<p>
Consider the unit sphere <m>x^2 + y^2 + z^2 = 1</m> with its spherical
parametrization
</p>

<men xml:id="eq-sphere-param">
\mathbf{r}(\theta,\phi)
= \langle \sin\phi\cos\theta,\, \sin\phi\sin\theta,\,
\cos\phi \rangle.
</men>

<p>
Our goal is to find the normal <m>\mathbf{n}</m> to the sphere at every
point on its surface. Holding one parameter fixed and varying the
other produces two tangent vectors,
</p>

<me>
\mathbf{r}_\phi = \frac{\partial \mathbf{r}}{\partial \phi}
= \lim_{\Delta\phi \to 0}
\frac{\mathbf{r}(\theta, \phi + \Delta\phi)
- \mathbf{r}(\theta,\phi)}{\Delta\phi},
\qquad
\mathbf{r}_\theta = \frac{\partial \mathbf{r}}{\partial \theta}
= \lim_{\Delta\theta \to 0}
\frac{\mathbf{r}(\theta + \Delta\theta, \phi)
- \mathbf{r}(\theta,\phi)}{\Delta\theta},
</me>

<p>
so that the two edges of a small coordinate patch on the sphere are
approximately <m>\mathbf{r}_\phi\, \Delta\phi</m> and
<m>\mathbf{r}_\theta\, \Delta\theta</m>, as in
<xref ref="fig-sphere-patch"/>. Their cross product is perpendicular
to the patch, so it gives a normal vector, and its length gives the
area of the patch:
</p>

<men xml:id="eq-normal-patch">
\mathbf{n} \approx (\mathbf{r}_\phi \times \mathbf{r}_\theta)\,
\Delta\phi\, \Delta\theta,
\qquad
\Delta\sigma \approx \lVert \mathbf{r}_\phi \times
\mathbf{r}_\theta \rVert\, \Delta\phi\, \Delta\theta.
</men>

<figure xml:id="fig-sphere-patch">
<caption>
A small coordinate patch on the unit sphere. The tangent vectors
<m>\mathbf{r}_\theta</m> and <m>\mathbf{r}_\phi</m> span the patch,
and their cross product points along the outward normal
<m>\hat{\mathbf{n}}</m>.
</caption>
<image xml:id="image-sphere-patch" width="80%">
<shortdescription>
A wireframe piece of a sphere with two tangent vectors and the normal
vector drawn at a point.
</shortdescription>
<description>
<p>
A wireframe octant of the unit sphere. At a point
<m>\mathbf{r}(\theta,\phi)</m> on the surface, a blue tangent
vector <m>\mathbf{r}_\theta</m> points east along a parallel, a red
tangent vector <m>\mathbf{r}_\phi</m> points south along a
meridian, a small shaded patch <m>\Delta\sigma</m> lies between them,
and a green arrow <m>\hat{\mathbf{n}}</m> points radially outward,
perpendicular to both.
</p>
</description>
<latex-image>
<![CDATA[\begin{tikzpicture}[line join=round, line cap=round]
  \tikzset{lbl/.style={fill=white, fill opacity=0.82, text opacity=1, inner sep=2.5pt, rounded corners=2pt}}
  \definecolor{oiblue}{RGB}{0,114,178}
  \definecolor{oivermillion}{RGB}{213,94,0}
  \definecolor{oigreen}{RGB}{0,158,115}
  \begin{axis}[
      view={115}{18},
      width=12cm,
      axis lines=center,
      axis equal image,
      axis line style={-latex, black!75, line width=0.7pt},
      xlabel={$x$}, ylabel={$y$}, zlabel={$z$},
      label style={font=\large},
      ticks=none,
      xmin=0, xmax=1.5, ymin=0, ymax=1.5, zmin=0, zmax=1.35,
      clip=false,
    ]
    % wireframe octant of the unit sphere
    \foreach \pp in {10,20,...,80}{
      \edef\tmp{\noexpand\addplot3[black!30, line width=0.4pt,
          domain=0:90, samples=37, samples y=0, forget plot]
        ({sin(\pp)*cos(x)}, {sin(\pp)*sin(x)}, {cos(\pp)});}\tmp}
    \foreach \tt in {0,15,...,90}{
      \edef\tmp{\noexpand\addplot3[black!30, line width=0.4pt,
          domain=0:90, samples=37, samples y=0, forget plot]
        ({sin(x)*cos(\tt)}, {sin(x)*sin(\tt)}, {cos(x)});}\tmp}
    % the patch between theta=35..55, phi=45..65 (shaded)
    \addplot3[surf, shader=flat, color=black!45, opacity=0.5,
              fill opacity=0.5,
              domain=35:55, samples=8, y domain=45:65, samples y=8,
              z buffer=sort, forget plot]
      ({sin(y)*cos(x)}, {sin(y)*sin(x)}, {cos(y)});
    % base point at theta=35, phi=45
    % r = (sin45 cos35, sin45 sin35, cos45) = (0.5792,0.4056,0.7071)
    \fill[black] (axis cs:0.5792,0.4056,0.7071) circle (1.4pt);
    % position vector from origin
    \draw[-latex, black!80, line width=1.1pt]
      (axis cs:0,0,0) -- (axis cs:0.5792,0.4056,0.7071);
    \node[lbl, font=\small, anchor=north, text=black!80]
      at (axis cs:0.34,0.18,0.38) {$\mathbf{r}(\theta,\phi)$};
    % r_theta = sinphi*(-sin t, cos t, 0) = 0.7071*(-0.5736,0.8192,0)
    \draw[-latex, oiblue, line width=1.5pt]
      (axis cs:0.5792,0.4056,0.7071)
      -- (axis cs:{0.5792-0.45*0.4056},{0.4056+0.45*0.5793},0.7071);
    \node[lbl, font=\normalsize, text=oiblue, anchor=west]
      at (axis cs:0.42,0.72,0.72) {$\mathbf{r}_\theta$};
    % r_phi = (cosphi cos t, cosphi sin t, -sinphi)
    %       = (0.7071*0.8192, 0.7071*0.5736, -0.7071)
    \draw[-latex, oivermillion, line width=1.5pt]
      (axis cs:0.5792,0.4056,0.7071)
      -- (axis cs:{0.5792+0.45*0.5793},{0.4056+0.45*0.4056},{0.7071-0.45*0.7071});
    \node[lbl, font=\normalsize, text=oivermillion, anchor=west]
      at (axis cs:0.85,0.62,0.42) {$\mathbf{r}_\phi$};
    % outward normal at the point (radial direction)
    \draw[-latex, oigreen!70!black, line width=1.6pt]
      (axis cs:0.5792,0.4056,0.7071)
      -- (axis cs:{0.5792*1.55},{0.4056*1.55},{0.7071*1.55});
    \node[lbl, font=\normalsize, text=oigreen!70!black, anchor=south west]
      at (axis cs:0.9,0.63,1.1) {$\hat{\mathbf{n}}$};
    \node[lbl, font=\small] at (axis cs:0.72,0.62,0.66) {$\Delta\sigma$};
  \end{axis}
\end{tikzpicture}]]>
</latex-image>
</image>
</figure>

<p>
Carry out this computation for the sphere: differentiate
<xref ref="eq-sphere-param"/> to get <m>\mathbf{r}_\phi</m> and
<m>\mathbf{r}_\theta</m>, take their cross product, and simplify it
using <m>\cos^2\theta + \sin^2\theta = 1</m>; you should be able to
write the result as a multiple of <m>\langle x, y, z\rangle</m>. Then
normalize, using the fact that <m>x^2+y^2+z^2 = 1</m> on the sphere,
and say in words which way the unit normal points. Compare with
<xref ref="fig-surface-normal-video"/>.
</p>

<sidebyside width="97%">
<image>
<latex-image>
\begin{tikzpicture}
\draw[rounded corners=8pt, draw=gray!55, thick] (0,0) rectangle (15,14);
\draw[gray!35] (0,11) -- (15,11);
\draw[gray!35] (0,5) -- (15,5);
\node[anchor=north west, font=\small\itshape] at (0.35,13.7) {$\mathbf r_\phi =$ \qquad\qquad $\mathbf r_\theta =$};
\node[anchor=north west, font=\small\itshape] at (0.35,10.7) {$\mathbf r_\phi \times \mathbf r_\theta =$};
\node[anchor=north west, font=\small\itshape] at (0.35,4.7) {$\hat{\mathbf n} =$ \qquad which points};
\end{tikzpicture}
</latex-image>
<shortdescription>Blank box in three rows, for the two tangent vectors, for their cross product, and for the unit normal with its direction.</shortdescription>
</image>
</sidebyside>

<assemblage xml:id="assemblage-sphere-normal">
<title>The Normal to the Unit Sphere</title>
<p>
For the parametrization <xref ref="eq-sphere-param"/> of the unit
sphere,
<md>
<mrow xml:id="eq-sphere-cross" number="yes">
\mathbf{r}_\phi \times \mathbf{r}_\theta
= \sin\phi\, \langle \sin\phi\cos\theta,\,
\sin\phi\sin\theta,\, \cos\phi \rangle
= \sin\phi\, \langle x, y, z \rangle,
</mrow>
</md>
and the unit normal is
<md>
<mrow xml:id="eq-sphere-unit-normal" number="yes">
\hat{\mathbf{n}} = \langle x, y, z \rangle
= \langle \sin\phi\cos\theta,\, \sin\phi\sin\theta,\, \cos\phi \rangle,
</mrow>
</md>
the position vector of the point itself: it points radially outward.
</p>
</assemblage>

<figure xml:id="fig-surface-normal-video">
<caption>
The tangent vectors <m>\mathbf{r}_\theta</m> and
<m>\mathbf{r}_\phi</m> span a small patch of the sphere; their cross
product is normal to the surface and its length measures the patch
area.
</caption>
<video preview="play-button.png" xml:id="video-surface-normal"
       source="videos/surface_normal_1080p.mp4"
       width="70%" aspect="16:9"/>
</figure>

</subsection>

<!-- ================================================================ -->
<subsection xml:id="sec-oriented-surfaces">
<title>Oriented Surfaces</title>

<p>
For a <term>closed surface</term>, that is, a surface that is the
boundary of a solid region <m>E</m>, the convention is that the
<term>positive orientation</term> is the one for which the normal
vectors point <em>outward</em> from <m>E</m>, and inward-pointing
normals give the negative orientation.
</p>

<example xml:id="example-cylinder-orientation">
<title>Two Orientations of a Cylinder</title>
<statement>
<p>
Consider the cylinder <m>x^2 + y^2 = 1</m>. Describe its two possible
orientations.
</p>
<p>
<em>Solution.</em>
(Describe the two normal vectors in words, with reference to the
<m>z</m>-axis, and say how they are related; see
<xref ref="fig-cylinder-normals"/>.)
</p>

<sidebyside width="97%">
<image>
<latex-image>
\begin{tikzpicture}
\draw[rounded corners=8pt, draw=gray!55, thick] (0,0) rectangle (15,5);
\node[anchor=north west, font=\small\itshape] at (0.35,4.7) {$\mathbf n_1$: \qquad\qquad $\mathbf n_2$: \qquad\qquad relation:};
\end{tikzpicture}
</latex-image>
<shortdescription>Blank box for describing the two orientations of the cylinder and how they are related.</shortdescription>
</image>
</sidebyside>
</statement>
</example>

<figure xml:id="fig-cylinder-normals">
<caption>
The cylinder <m>x^2 + y^2 = 1</m> with its two possible orientations:
<m>\mathbf{n}_1</m> points away from the <m>z</m>-axis and
<m>\mathbf{n}_2 = -\mathbf{n}_1</m> points toward it.
</caption>
<image xml:id="image-cylinder-normals" width="55%">
<shortdescription>
A cylinder around the vertical axis with an outward normal arrow and
an inward normal arrow at the same point.
</shortdescription>
<description>
<p>
A translucent cylinder of radius one around the <m>z</m>-axis. At a
point on its curved surface, a magenta arrow <m>\mathbf{n}_1</m>
points horizontally away from the axis, and a teal arrow
<m>\mathbf{n}_2</m> points from the same point horizontally toward
the axis.
</p>
</description>
<latex-image>
<![CDATA[\begin{tikzpicture}[line join=round, line cap=round]
  \tikzset{lbl/.style={fill=white, fill opacity=0.82, text opacity=1, inner sep=2.5pt, rounded corners=2pt}}
  \definecolor{oiblue}{RGB}{0,114,178}
  \definecolor{oimagenta}{RGB}{204,0,119}
  \definecolor{oiteal}{RGB}{0,158,115}
  \begin{axis}[
      view={115}{18},
      width=9.5cm,
      axis lines=center,
      axis equal image,
      axis line style={-latex, black!75, line width=0.7pt},
      xlabel={$x$}, ylabel={$y$}, zlabel={$z$},
      label style={font=\large},
      ticks=none,
      xmin=-1.6, xmax=1.6, ymin=-2.6, ymax=2.6, zmin=-1.5, zmax=1.9,
      clip=false,
    ]
    % cylinder surface
    \addplot3[surf, shader=interp,
              point meta={cos(x)},
              colormap={softb}{color=(oiblue!10!white) color=(oiblue!38!white)},
              opacity=0.5, fill opacity=0.5,
              domain=0:360, samples=49,
              y domain=-1.2:1.4, samples y=2,
              z buffer=sort, forget plot]
      ({cos(x)}, {sin(x)}, {y});
    % rims
    \addplot3[oiblue!60, line width=0.8pt, domain=0:360, samples=73,
        samples y=0, forget plot] ({cos(x)}, {sin(x)}, {1.4});
    \addplot3[oiblue!60, line width=0.8pt, domain=0:360, samples=73,
        samples y=0, forget plot] ({cos(x)}, {sin(x)}, {-1.2});
    % normals at the point (0,1,0.2)
    \fill[black] (axis cs:0,1,0.2) circle (1.4pt);
    \draw[-latex, oimagenta, line width=1.6pt]
      (axis cs:0,1,0.2) -- (axis cs:0,2.0,0.2);
    \node[lbl, font=\normalsize, text=oimagenta, anchor=south]
      at (axis cs:0,1.85,0.32) {$\mathbf{n}_1$};
    \draw[-latex, oiteal!80!black, line width=1.6pt]
      (axis cs:0,1,0.2) -- (axis cs:0,0.15,0.2);
    \node[lbl, font=\normalsize, text=oiteal!80!black, anchor=south]
      at (axis cs:0,0.42,0.34) {$\mathbf{n}_2$};
    \node[lbl, font=\normalsize, text=oiblue] at (axis cs:-1.0,-0.85,1.0)
      {$x^2+y^2=1$};
  \end{axis}
\end{tikzpicture}]]>
</latex-image>
</image>
</figure>

</subsection>

<!-- ================================================================ -->
<subsection xml:id="sec-surface-integral-def">
<title>The Surface Integral of a Vector Field</title>

<p>
The following definition should be compared to the definition of the
line integral of a vector field <m>\mathbf{F}</m> along a curve
<m>C</m>, i.e. <m>\int_C \mathbf{F} \cdot \mathbf{T}\, ds</m>, where
<m>\mathbf{T}</m> was defined as the unit tangent vector to the curve
<m>C</m>. For a surface, the unit tangent is replaced by the unit
normal.
</p>

<definition xml:id="def-surface-integral">
<statement>
<p>
If <m>\mathbf{F}</m> is a continuous vector field defined on an
oriented surface <m>S</m> with unit normal vector
<m>\hat{\mathbf{n}}</m>, then the <term>surface integral of
<m>\mathbf{F}</m> over <m>S</m></term> is
</p>

<men xml:id="eq-flux-def">
\iint_S \mathbf{F} \cdot d\mathbf{S}
= \iint_S \mathbf{F} \cdot \hat{\mathbf{n}}\, d\sigma.
</men>

<p>
This integral is also called the <term>flux</term> of
<m>\mathbf{F}</m> across <m>S</m>.
</p>
</statement>
</definition>

<remark xml:id="remark-flux-param">
<p>
If the surface <m>S</m> is parametrized with respect to <m>u</m> and
<m>v</m>, we can write the unit normal vector as
<m>\hat{\mathbf{n}} = \dfrac{\mathbf{r}_u \times \mathbf{r}_v}
{\lVert \mathbf{r}_u \times \mathbf{r}_v \rVert}</m>, which can be
combined with the fact that
<m>d\sigma = \lVert \mathbf{r}_u \times \mathbf{r}_v \rVert\,
du\, dv</m> (from <xref ref="eq-normal-patch"/>) to write the surface
integral as
</p>

<men xml:id="eq-flux-param">
\iint_S \mathbf{F} \cdot \hat{\mathbf{n}}\, d\sigma
= \iint_R \mathbf{F} \cdot (\mathbf{r}_u \times \mathbf{r}_v)\,
du\, dv,
</men>

<p>
where <m>R</m> is the region of the parameters in the
<m>uv</m>-plane. The normalizing length cancels against the area
factor, which is what makes this formula so computable.
</p>
</remark>

<p>
For instance, if the vector field <m>\mathbf{F}</m> is the velocity
field of a fluid, then the surface integral of <m>\mathbf{F}</m> over
the surface <m>S</m> is the net rate at which fluid is crossing
<m>S</m> in the chosen positive direction. As another example, if the
vector field <m>\mathbf{F}</m> represents the electric field due to a
charged particle such as an electron, then the surface integral of
<m>\mathbf{F}</m> over the surface <m>S</m> corresponds to the flux of
the electric field through the surface <m>S</m>.
</p>

<example xml:id="example-sphere-flux">
<title>Flux Across the Unit Sphere</title>
<statement>
<p>
Find the flux of the vector field
<m>\mathbf{F}(x,y,z) = z\,\mathbf{i} + y\,\mathbf{j} +
x\,\mathbf{k}</m> across the unit sphere
<m>x^2 + y^2 + z^2 = 1</m>.
</p>
<p>
<em>Solution.</em>
(Use the spherical parametrization <xref ref="eq-sphere-param"/> with
its parameter ranges, so that <xref ref="eq-flux-param"/> applies; the
cross product is <xref ref="eq-sphere-cross"/>, and check that it
points outward. Substitute the parametrization into <m>\mathbf{F}</m>,
dot, and integrate over the parameter rectangle; one of the two terms
integrates to zero over a full period of <m>\theta</m>. Compare with
<xref ref="fig-sphere-flux-video"/>.)
</p>

<sidebyside width="97%">
<image>
<latex-image>
\begin{tikzpicture}
\draw[rounded corners=8pt, draw=gray!55, thick] (0,0) rectangle (15,13);
\draw[gray!35] (0,10.5) -- (15,10.5);
\draw[gray!35] (0,8) -- (15,8);
\draw[gray!35] (0,5) -- (15,5);
\node[anchor=north west, font=\small\itshape] at (0.35,12.7) {$\mathbf r_\phi \times \mathbf r_\theta =$ \qquad (outward because)};
\node[anchor=north west, font=\small\itshape] at (0.35,10.2) {$\mathbf F(\theta,\phi) =$};
\node[anchor=north west, font=\small\itshape] at (0.35,7.7) {$\mathbf F \cdot (\mathbf r_\phi \times \mathbf r_\theta) =$};
\node[anchor=north west, font=\small\itshape] at (0.35,4.7) {$\displaystyle\iint_S \mathbf F\cdot\hat{\mathbf n}\,d\sigma = \int_0^{2\pi}\!\!\int_0^{\pi}$};
\end{tikzpicture}
</latex-image>
<shortdescription>Blank box in four rows, for the cross product and its orientation, for the field along the sphere, for the integrand, and for the flux integral and its evaluation.</shortdescription>
</image>
</sidebyside>
</statement>
</example>

<figure xml:id="fig-sphere-flux-video">
<caption>
The field <m>\mathbf{F} = z\,\mathbf{i} + y\,\mathbf{j} +
x\,\mathbf{k}</m> crossing the unit sphere. Use it to check the flux
you computed.
</caption>
<video preview="play-button.png" xml:id="video-sphere-flux"
       source="videos/sphere_flux_1080p.mp4"
       width="70%" aspect="16:9"/>
</figure>

<example xml:id="example-paraboloid-flux">
<title>Flux Across a Closed Surface</title>
<statement>
<p>
Evaluate the surface integral
<m>\displaystyle \iint_S \mathbf{F} \cdot \hat{\mathbf{n}}\,
d\sigma</m>, where
<m>\mathbf{F}(x,y,z) = y\,\mathbf{i} + x\,\mathbf{j} +
z\,\mathbf{k}</m> and the surface <m>S</m> is the boundary of the
solid region <m>E</m> enclosed by the paraboloid
<m>z = 1 - x^2 - y^2</m> and the plane <m>z = 0</m>.
</p>
<p>
<em>Solution.</em>
(The closed surface has two pieces, shown in
<xref ref="fig-paraboloid-flux"/>: the paraboloid cap <m>S_1</m> and
the unit disk <m>S_2</m> in the plane <m>z = 0</m>. Which way must the
outward normal point on each? Parametrize the cap as a graph,
<m>\mathbf{r}(x,y) = \langle x, y, 1 - x^2 - y^2\rangle</m>, and
compute <m>\mathbf{r}_x \times \mathbf{r}_y</m>; check its
<m>z</m>-component against the required orientation. Do the same for
the disk, fixing the sign if necessary. Then apply
<xref ref="eq-flux-param"/> to each piece over the unit disk <m>R</m>;
on the cap, one term vanishes by symmetry and polar coordinates finish
the rest. Compare with <xref ref="fig-paraboloid-flux-video"/>.)
</p>

<sidebyside width="97%">
<image>
<latex-image>
\begin{tikzpicture}
\draw[rounded corners=8pt, draw=gray!55, thick] (0,0) rectangle (15,17);
\draw[gray!35] (0,14) -- (15,14);
\draw[gray!35] (0,11.5) -- (15,11.5);
\draw[gray!35] (0,6) -- (15,6);
\draw[gray!35] (0,2.5) -- (15,2.5);
\node[anchor=north west, font=\small\itshape] at (0.35,16.7) {on $S_1$: $\mathbf n_1 = \mathbf r_x \times \mathbf r_y =$ \qquad orientation check:};
\node[anchor=north west, font=\small\itshape] at (0.35,13.7) {on $S_2$: $\mathbf n_2 =$};
\node[anchor=north west, font=\small\itshape] at (0.35,11.2) {$\displaystyle\iint_{S_1} \mathbf F\cdot\hat{\mathbf n}\,d\sigma = \iint_R$};
\node[anchor=north west, font=\small\itshape] at (0.35,5.7) {$\displaystyle\iint_{S_2} \mathbf F\cdot\hat{\mathbf n}\,d\sigma =$};
\node[anchor=north west, font=\small\itshape] at (0.35,2.2) {total:};
\end{tikzpicture}
</latex-image>
<shortdescription>Blank box in five rows, for the normal to the cap with its orientation check, for the normal to the disk, for the flux through the cap, for the flux through the disk, and for the total.</shortdescription>
</image>
</sidebyside>
</statement>
</example>

<figure xml:id="fig-paraboloid-flux-video">
<caption>
The closed surface made of the paraboloid cap <m>S_1</m> and the disk
<m>S_2</m>, with outward normals. Use it to check the flux of
<m>\mathbf{F} = y\,\mathbf{i} + x\,\mathbf{j} + z\,\mathbf{k}</m>
through each piece.
</caption>
<video preview="play-button.png" xml:id="video-paraboloid-flux"
       source="videos/paraboloid_flux_1080p.mp4"
       width="70%" aspect="16:9"/>
</figure>

<figure xml:id="fig-paraboloid-flux">
<caption>
The boundary <m>S</m> of the solid enclosed by
<m>z = 1 - x^2 - y^2</m> and <m>z = 0</m>: the cap <m>S_1</m> with
upward-and-outward normals, and the disk <m>S_2</m> with downward
normals.
</caption>
<image xml:id="image-paraboloid-flux" width="75%">
<shortdescription>
A paraboloid dome over a disk, with outward normal arrows on the dome
and downward normal arrows on the disk.
</shortdescription>
<description>
<p>
A translucent blue dome, the paraboloid <m>z = 1 - x^2 - y^2</m>,
labeled <m>S_1</m>, sits over the unit disk in the <m>xy</m>-plane,
labeled <m>S_2</m>. Magenta arrows on the dome point up and outward,
and magenta arrows below the disk point straight down: together they
give the closed surface its positive, outward orientation.
</p>
</description>
<latex-image>
<![CDATA[\begin{tikzpicture}[line join=round, line cap=round]
  \tikzset{lbl/.style={fill=white, fill opacity=0.82, text opacity=1, inner sep=2.5pt, rounded corners=2pt}}
  \definecolor{oiblue}{RGB}{0,114,178}
  \definecolor{oimagenta}{RGB}{204,0,119}
  \begin{axis}[
      view={115}{18},
      width=11.5cm,
      axis lines=center,
      axis equal image,
      axis line style={-latex, black!75, line width=0.7pt},
      xlabel={$x$}, ylabel={$y$}, zlabel={$z$},
      label style={font=\large},
      ticks=none,
      xmin=-1.6, xmax=1.6, ymin=-1.9, ymax=1.9, zmin=-0.7, zmax=1.7,
      clip=false,
    ]
    % paraboloid cap
    \addplot3[surf, shader=interp,
              point meta={z},
              colormap={softb}{color=(oiblue!22!white) color=(oiblue!60!white)},
              opacity=0.6, fill opacity=0.6,
              domain=0:360, samples=49,
              y domain=0:1, samples y=17,
              z buffer=sort, forget plot]
      ({y*cos(x)}, {y*sin(x)}, {1 - y^2});
    % rim
    \addplot3[oiblue, line width=1.2pt, domain=0:360, samples=73,
        samples y=0, forget plot] ({cos(x)}, {sin(x)}, {0});
    % outward normals on the cap: n = (2x, 2y, 1) scaled
    \foreach \tt in {20,110,200,290}{
      \edef\tmp{\noexpand\draw[-latex, oimagenta, line width=1.3pt]
        (axis cs:{0.7*cos(\tt)},{0.7*sin(\tt)},{0.51})
        -- (axis cs:{0.7*cos(\tt)+0.32*1.4*cos(\tt)},
            {0.7*sin(\tt)+0.32*1.4*sin(\tt)},{0.51+0.32});}\tmp}
    % apex normal (straight up)
    \draw[-latex, oimagenta, line width=1.3pt]
      (axis cs:0,0,1) -- (axis cs:0,0,1.45);
    % downward normals under the disk
    \foreach \tt in {45,165,285}{
      \edef\tmp{\noexpand\draw[-latex, oimagenta, line width=1.3pt]
        (axis cs:{0.55*cos(\tt)},{0.55*sin(\tt)},{0})
        -- (axis cs:{0.55*cos(\tt)},{0.55*sin(\tt)},{-0.5});}\tmp}
    % labels
    \node[lbl, font=\normalsize, text=oiblue] at (axis cs:-0.95,-0.8,0.85) {$S_1$};
    \node[lbl, font=\normalsize] at (axis cs:-0.35,0.5,0.06) {$S_2$};
  \end{axis}
\end{tikzpicture}]]>
</latex-image>
</image>
</figure>

</subsection>

</section>
